# 变更与自我检查

每次重要变更都应记录：目的、影响文件、验证命令、已知限制。

安全检查清单：
- [ ] 用户身份来自已认证请求，不信任任意 query user_id。
- [ ] 文件路径 resolved 后仍在允许工作区。
- [ ] 工具调用有稳定调用 ID 和幂等或副作用分类。
- [ ] 关键状态先持久化，再广播。
- [ ] 刷新恢复使用按 chat/msg 绑定的 cursor，不覆盖其他会话。
- [ ] 失败不会静默降级为空列表。
- [ ] 前端资源变更后运行 tools/build-index.py。
