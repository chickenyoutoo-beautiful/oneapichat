# OneAPIChat 项目架构指南

> 每次修改项目后，Agent 必须更新此文件（特别是「最近变更」章节）。
> **规范**: 新变更只需在「最近变更」顶部追加**一行摘要**（日期+emoji+标题+核心要点），完整细节写入 `docs/CHANGELOG.md`（「OneAPIChat 最近变更全文」章节）。

## 铁律（Agent 必读）

### 🚫 index.html script 标签 — 禁止手动编辑

**Agent 修改任何 JS/CSS 文件后，必须运行 `python3 tools/build-index.py`，严禁手动修改 index.html 中的 `<script>` / `<link>` 标签。**

原因: index.html 有 45 个资源标签，手动改必错。生成器会:
- 扫描 `js/*.js` / `css/*.css` / `lib/*`，按文件 mtime 自动生成 `?v=` 版本号
- 替换 `<!-- AUTO-GENERATED-START --> ... <!-- AUTO-GENERATED-END -->` 之间的内容
- 自动同步 `public/index.html` → 根 `index.html`

```bash
python3 tools/build-index.py            # 重建
python3 tools/build-index.py --check    # 检查是否过期
python3 tools/build-index.py --watch    # 监听变化自动重建
```

**错误示范（会导致版本不一致）**:
- ❌ 手动改 1 个 `?v=` 而忘记其他 44 个
- ❌ 改 `public/index.html` 但不同步到根（nginx serve 根文件）

**正确流程**:
1. 修改 `js/xxx.js` 或 `css/xxx.css`
2. 运行 `python3 tools/build-index.py`
3. 提交代码

### 项目文件结构

- **nginx serve 路径**: `/var/www/html/oneapichat/index.html`（根目录）
- **实际源文件**: `public/index.html`（generator 写入这里，然后同步到根）
- **symlink 结构**: 根 `js`→`public/js`, `css`→`public/css`, `lib`→`public/lib/lib`

## 最近变更

- **2026-08-08**: 🐛 **LongCat 推理面板不显示修复 (thinking 参数格式错误)** — 根因: `getThinkingIntensityParams` 中 `isClaude = isAnthropicFormat && n.indexOf('claude')===0`, LongCat 在 Anthropic 格式下模型名以 `longcat` 开头不是 `claude`, 导致走到 OpenAI/DeepSeek 分支返回 `{reasoning_effort:'medium'}`, 但 LongCat Anthropic 端点只接受 `thinking:{type:'enabled'}`, thinking 从未开启, API 自然不返回 reasoning_content; 修复: 将 `isLongCat` 判断移到 `isClaude` 之前, 无论何种格式都返回 `{thinking:{type:'enabled'}}`; 同时修复 retry 路径 (RS/HTTP) 不再丢弃已有 reasoning; models.js + main.js + index.html `?v=` bump
- **2026-08-08**: 🎨 **引擎状态面板 UI 全面重构 (设计系统一致性)** — 原面板全内联样式+Tailwind工具类混用(按钮`px-3 py-1.5`、图标`label-icon`、边框`dark:border-gray-700`类失效); 重构: 新增`.engine-status-panel`(圆角卡片+双主题背景边框) / `.engine-status-header`(标题行+刷新按钮右对齐) / `.engine-refresh-btn`(独立按钮样式+loading旋转动画) / `.engine-health-card`(健康状态卡片) / `.engine-section-label`(带图标分区标签) / `.engine-empty-hint` / `.engine-cron-delete`; JS 刷新时按钮加 loading 类(spinner动画) + finally 移除; 健康文本去 emoji 改用圆点色标; 3文件+CSS, index.html `?v=` bump
- **2026-08-08**: 🐛 **刷新后接续气泡三点残留修复 (resume typing 类泄漏)** — 根因: resume 路径创建气泡时添加 `typing` 类但首个内容块到达时未移除(正常路径 main.js:2749 有移除); `cleanupStreamState()` 的 `if(!st) return;` 早退守卫导致 JSON 快径未创建 `_streamState` 时 `typing` 永不清除; 修复: `applyStreamRender()` 首次创建 state 时立即移除 `typing` + `cleanupStreamState()` 早退时仍检查 `activeBubbleMap` 清理; index.html `?v=` bump
- **2026-08-08**: 🚪 **关闭Agent模式时计划面板带入普通会话修复** — 根因: `#flowPanel` 位于输入区(`.input-clip`)而非消息区(`#chatBox`), `loadChat` 重绘消息不影响它, 必须主动 dismiss; 原逻辑仅在 `prevMode==='plan'` 时 dismiss(plan→off), 但 `createFlowPanel` 在 agent/yolo/plan 任一模式下都可被 `plan_update(action="create")` 触发, 导致 agent→off / yolo→off 时面板残留普通会话; 修复: 拆分为 plan 状态重置(保持仅 plan→off) + 面板 dismiss(统一 `mode==='off' && !tempAgentGranted` 时触发); agent.js + index.html `?v=` bump
- **2026-08-08**: 🔔 **子代理运行状态条 (Agent模式下子代理运行中实时可见)** — 此前Agent模式创建子代理后用户看不到运行进度(流程面板仅Plan模式显示,侧栏自动收起); 新增 `#subAgentStatusBar` 状态条: 旋转spinner+「子代理运行中」+实时工具/步骤+完成计数; 接线: addAgentToTask→显示 / pushAgentResultToTask→更新 / agent:step→实时工具步骤 / _checkTaskCompletion全部完成→「子代理已完成」2s后消失; 3文件+CSS, index.html `?v=` bump
- **2026-08-08**: 🧠 **LongCat/Anthropic 推理面板不可见修复 (三 bug 叠加 + OpenAI 路径统一)** — 根因: ①Anthropic 路径 `_parseAnthropicResponse` 的 `thinking_delta` 只存 `pendingMsg.reasoning` 不实时渲染; ②流结束后不补渲染; ③自动重试逻辑 `_retryLongCatWithoutThinking`/RS 路径主动删除推理面板+清空内容; 修复: stream-handler.js 新增 `window._ensureReasoningPanel` 公共辅助函数,统一 OpenAI 路径两处内联创建; main.js Anthropic 路径 `thinking_delta` 加实时渲染+流结束后补渲染; 三处自动重试逻辑(LongCat/RS/工具降级)停止清除推理; index.html `?v=` bump
- **2026-08-06**: 🛡️ **503 auth_unavailable 瞬态错误自动重试 (RS + 直连双路径)** — CLIProxyAPI 返回 503 auth_unavailable 时自动重试最多 3 次 (2s/3s/4.5s 递增退避); RS 路径: resume-stream.js 新增 `_isRetriableError()` 检测 (auth_unavailable/429/502/504) + `_transientRetryCount` 计数 + 成功后清理; 直连路径: main.js catch 块新增 503 重试分支 (window 级计数防递归); 重试次数用尽后走原有错误展示; index.html `?v=` bump
- **2026-08-06**: 🔧 **学习通刷课模型列表刷新按钮无效修复 (CORS)** — 根因: chaoxing.html 单文件不加载 config.js → proxyFetch 不可用 → 直接 fetch 外部 API 被 CORS 静默拦截; 新增 `api/chaoxing_api.php?action=models_proxy` 服务端 curl 中继绕过 CORS + 按钮 loading 反馈 + 错误信息优化
- **2026-08-06**: 🔒 **learning_records.db 只读复发 + setgid 加固** — git 重建文件后所有权重置为 naujtrats:naujtrats 644 → www-data 只读; 修复: chown www-data:www-data + chmod 664 + **chmod g+s 设 setgid 位**（新建文件自动继承 www-data 组防复发）
- **2026-08-06**: 🚨 **聊天数据丢失根因修复 (31条普通聊天恢复 + 双重防护)** — 根因: 前端 `beaconSaveChats`(页面关闭时 sendBeacon)只发本地 chats 到服务器,服务器 chat.php POST all 直接覆盖写入无合并保护; 当 restoreUserData 清理逻辑因多标签页竞态误删本地普通聊天后, 页面关闭时将仅存的 10 条 Agent 聊天覆盖服务器, 丢失 31 条普通聊天; 修复: ①服务器端 POST all 合并保护(服务器比前端多5条以上时取并集, 跳过已墓碑标记的删除项) ②前端 beaconSaveChats 本地<5条时跳过发送 ③从备份 all.json.20260805-1644 恢复 32 条聊天(33普通+53Agent=86条); chat.php + storage.js 改动, index.html `?v=` bump
- **2026-08-08**: 🔧 **index.html 资源标签自动生成器 (根治 agent 改错)** — 新增 `tools/build-index.py`，扫描 js/css/lib 按 mtime 自动生成 45 个 script/link 的 `?v=`，替换 AUTO-GENERATED 区块；CLAUDE.md 加铁律禁止手改；解决双文件不同步、4 种版本号并存、agent 漏改的根本问题
- **2026-08-05**: 🎨 **画布UI优化 (空间释放 + 浮层交互)** — 信息面板从画布移除，改为点击"创建变体"按钮弹出居中浮层(显示原始提示词+修改指令输入); 画布只保留图片+底部胶囊操作栏+缩略图条; 浮层带动画弹出+毛玻璃阴影; 图片获得完整展示空间; index.html 41个 `?v=` bump 1786051000
- **2026-08-06**: 🎯 **模型选择刷新/取消后跳回首个模型修复 (三重根因)** — ①`snapshotConfig()`(ui.js)快照键列表缺 `baseUrlProvider` 和 `model_{provider}` 键,取消时厂商未被还原 + 通用 `model` 键回退到旧厂商模型名 → `initializeConfig()` 读到 `model_{新厂商}` 为空 → fallback 到旧厂商模型名 → 新厂商列表中找不到 → 显示首个; 修复: 快照/恢复时遍历 localStorage 动态捕获 `baseUrlProvider` + 所有 `model_*` 键; ②`onProviderChange()`(utils.js)切到新厂商无已存模型时只写通用 `model` 键不写 `model_{provider}`,导致下次切回时 per-provider 记忆为空; 修复: 同时写入 `model_{provider}` 默认值; ③`fetchModels()`(config.js)设置选中值后未回写 localStorage,导致刷新后 `model_{provider}` 与实际显示不一致; 修复: 选中后同步写入 `model` + `model_{provider}`; 3 文件 + index.html `?v=` bump 1786050000
- **2026-08-06**: 🔧 **视觉理解"自定义"提供商从半成品补全为完整支持** — 此前选"自定义"后 UI 只剩模型输入框(Key/URL 全隐藏)、运行时走 MiniMax MCP 回退(不读自定义配置)、`_analyzeImagesWithVisionProvider` 直接 return null 跳过 custom；参照图像生成的 custom 模式补全 6 环节: ①index.html 新增 visionCustomKeyField/visionCustomUrlField; ②ui.js onVisionProviderChange 添加 custom 字段映射+独立键恢复; ③config.js saveConfig 保存 visionApiKeyCustom/visionApiUrlCustom; ④init.js 恢复自定义字段+_visFields 加 custom 映射; ⑤config.php allowedKeys 加两个自定义键; ⑥tools-exec.js analyze_image 添加 custom 直连分支(与 xai/openai 同路径); ⑦image-gen.js analyzeImage 感知提供商读自定义键; ⑧utils.js 预分析支持 custom; index.html 40 个 `?v=` bump 1786040000
- **2026-08-06**: 🖼️ **图片生成重复渲染修复 (3→9 根因) + 冗余占位块移除 + 刷新虚假日志恢复修复** — ①移除 main.js 中 `_isImageModel` 路径的"正在生成图片..."占位块(带计时器的冗余块), 保留 tools-exec.js 中有渐变动画背景的占位块; ②并行生成 3 张图片时每张重复 3 遍根因: for 循环内每次迭代从 `pendingMsg.generatedImages` 取全部图片渲染, 去重用的 `_domSrcs` 读 DOM 但实际插入在 setTimeout 延迟执行 → 3 轮迭代各插 3 张 = 9 张; 修复: 新增跨迭代持久化 `_scheduledImgSrcs` Set; ③刷新后进入"生成中"状态根因: 非链式工具循环路径缺 `localStorage.removeItem('_savedPartial')`, 刷新时 loadChat 误触发虚假恢复; 修复: 在 `delete pendingMsg.partial` 后补上清理; main.js + index.html `?v=` bump 1786030000
- **2026-08-05**: 🎨 **配置面板 emoji → SVG 图标全面替换** — 配置栏/设置面板中绝大部分 emoji 替换为 Feather-style SVG 图标: ①index.html 思考强度(🧠)/可恢复流式(🧪)/链式输出(🔗)/工具调用模式(🔧)/刷课汇报(📊)/代理模式(🌐)/Anthropic格式(🔷)/刷新引擎(🔄)/AI生成(🤖)/保存技能(💾)/代理已就绪(✅) 全部改 inline SVG; ②config.js 用量统计(💰/🔤/💾/📤/📥/🔧/✅/❌)、MCP 服务器状态点(🟢🟡🔴)、代理节点状态、技能生成按钮/状态、保存配置/复制/连接 toast 全部改 `_icon()` 工厂; ③`_icon()` 新增 flask/link/barchart/bot/refresh/save/dollar/hash/database/login/logout/check/x/plus/sparkle/eye/eyeoff/warning 18 个图标; ④CSS 新增 `.label-icon`/`.usage-icon`/`.btn-icon`/`.status-icon`/`.toast-icon-inline`/`.mcp-*-icon`/`.proxy-status-icon` 等样式类; 仅 confirm() 原生弹窗内 emoji 无法替换故保留; 3 文件 + CSS 改动
- **2026-08-05**: ⚡ **统一并行工具执行框架 (v4.0)** — 取代原有的按工具类型单独并行(web_search/generate_image), 提供通用并行机制: ①核心原理=模型同一轮发出的工具调用本质上独立, 默认全部并行; ②`_sequentialOnlyTools` 排除集(video_download/bili_download/video_upload_cloudreve/cr_upload_file/cr_create_share/server_file_write/server_file_append 共 8 个); ③预扫描收集可并行索引(≥2触发); ④`Promise.all` 预执行全部可并行调用(独立 AbortController); ⑤for 循环从 `_parallelToolResults[index]` 取结果; ⑥`window.__parallelToolActive` 通用标记替换原 `__parallelImageActive`; ⑦`showSearchTicker` 改为 IIFE 累积模式(rAF 合并同批多次调用为一条滚动条, 最多 18 行, URL 去重); ⑧滚动条高度 26px→84px (可见 ~3.2 行), 新增上下渐隐 mask + 左右渐隐保留; 3 JS + CSS, index.html 41 个 `?v=` bump 1786020000
- **2026-08-05**: 🎨 **画布模式全面重构 (精致设计 + 指令修改)** — 背景改为 `linear-gradient(135deg,#1e2030,#1a1c2e,#15172a)`; 右侧信息面板(原始提示词只读+修改指令输入+元信息); 底部浮动毛玻璃胶囊操作栏(下载/复制/以图生图); 缩略图条居中+圆角+精致悬停; 提示词改为只读+独立"修改指令"输入框，用户告诉AI怎么改(如"把背景改成蓝色")，生成变体时拼接指令到提示词; 顶部可收起面板按钮; index.html `?v=` bump 1786041000
- **2026-08-05**: 🔍 **web_search 并行调用 + 滚动条渐隐优化** — 模型同一轮发出多个 web_search 时自动并行执行: ①main.js 进入 for 前扫描连续 web_search 调用(≥2触发); ②`Promise.all` 预执行全部搜索(各自独立超时); ③for 循环内从 `_parallelWebSearchResults[index]` 取结果跳过重执行 + 调 `showSearchTicker` 展示; ④`showSearchTicker` 改为 IIFE 累积模式(rAF 合并同批多次调用为一条滚动条, 最多 18 行, URL 去重); ⑤滚动条高度 26px→84px (可见 ~3.2 行), 新增上下渐隐 mask + 左右渐隐保留; 3 文件 + CSS, index.html 41 个 `?v=` bump 1786010000
- **2026-08-05**: 🖼️ **AI 生成图片画布增强 Phase 2 (多图预览 + 评论修改)** — 参考 Codex 画布交互: 点击图片进入画布模式，底部缩略图预览条(过多时 +N 折叠)，右侧信息面板(提示词可编辑+重新生成、模型/尺寸/时间只读、备注可编辑自动保存); 新增 `collectChatImages()` 收集聊天所有图片; `buildImageMeta` 新增 `notes` 字段; 重写 `showImageLightbox` 为画布模式; CSS 新增 .canvas-filmstrip/.canvas-info-panel 等样式; index.html 41 个 `?v=` bump 1786001000
- **2026-08-05**: 🎨 **并行图片生成 (Promise.all 批次预执行)** — 模型同一轮发出多个 `generate_image`/`generate_image_i2i` 时自动并行执行: ①main.js 进入 for 前扫描连续图片调用索引(≥2触发); ②`Promise.all` 预执行全部调用(独立 AbortController); ③for 循环内从 `_parallelImageResults[index]` 取结果跳过重执行; ④`window.__parallelImageActive` 标记让 tools-exec.js 跳过个体占位符; ⑤工具定义 `n` 参数描述增加并行引导提示; 3 JS + 1 HTML, index.html 4 个 `?v=` bump 1785953000
- **2026-08-05**: 📋 **子代理会话侧边栏集成 (Agent 域 + 只读 + 自动同步)** — 子代理对话记录可从左侧侧边栏查看: ①`isAgentChat()`(core.js) 增加 `_agent_sub_` 前缀识别,子代理会话归入 Agent 视图; ②SSE `agent:result`(agent-notify.js) 完成时自动同步到 `chats` 对象; ③启动时(init.js)从 `localStorage` 恢复所有 `agent_chat_*` 到 `chats`; ④`sendMessage`(main.js) 子代理会话只读守卫; ⑤`loadChat`(dialogs.js) 跳过 RS 续接; ⑥`deleteAgent`/`clearAllAgents`(agent.js) 同步清理 chats 条目; ⑦`deleteChat`(dialogs.js) 子代理会话同步清 localStorage; ⑧`commands.js` `/agent` 命令设置 `_agentSub` 标记; 7 文件改动 + index.html 41 个 `?v=` bump 1785950000
- **2026-08-05**: 🛡️ **聊天加载崩溃修复 (generatedImage 对象格式兼容性)** — 根因: `storage.js:528` 服务器合并恢复图片数据时对 `_mm.generatedImage` 调用 `.indexOf('data:')`,但新格式是对象 `{url, prompt, model, aspect_ratio, timestamp}` 无此方法 → `restoreUserData` 抛异常 → 聊天加载失败 → 历史会话全部看不到; 修复: 提取 URL 时兼容字符串/对象两种格式 (`typeof === 'string' ? val : val.url`); index.html 41 个 `?v=` bump 1785951000
- **2026-08-05**: 🫧 **隐藏思考+链式输出下 websearch 气泡为空修复 (占位指示器)** — 根因: 隐藏思考(`body.hide-reasoning .reasoning-details{display:none}`)后,模型只输出推理+工具调用(无正文)时,气泡内仅剩隐藏的推理块+空 markdown-body → 视觉上呈空气泡; 修复: 流式/非流式流结束且`toolCalls>0 && !fullText`时,向 markdown-body 注入轻量占位元素`.tool-executing-placeholder`(脉冲点+「正在执行工具...」),main.js 工具卡片创建前移除; 顺手修复 handleNonStream 中 `_ownedBubble` 未定义 bug(改 `currentBubble`); 3 文件 + CSS, index.html 41 个 `?v=` bump 1785942182
- **2026-08-05**: 🛡️ **生成图片刷新后消失修复 (对象格式兼容性)** — 根因: `compressChatsForStorage` (dialogs.js) 对 `generatedImages` 调用 `.startsWith('data:')` 时,新格式对象无此方法 → TypeError → 保存失败 → 刷新后丢失; 修复: ① dialogs.js 兼容对象/字符串两种格式 ② stream-handler.js `handleNonStream` 返回统一对象格式 ③ main.js 所有 `startsWith`/`indexOf` 调用兼容对象格式 ④ 去重改用 URL 比对而非引用比对; index.html `?v=` bump 1785912000
- **2026-08-05**: 🖼️ **图像生成模型选择不保存修复 (per-provider 键同步)** — 根因: `saveConfig` (config.js) 只写通用 `imageModel` 键, 但 `toggleImageProviderFields` (ui.js) 读取 `imageModel_{provider}` 独立键 → 保存后刷新, 独立键为 null, init 时 `toggleImageProviderFields` 用默认值(如 openrouter 的 `openai/gpt-5.4-image-2`)覆盖已恢复的输入框; 修复三处: ①config.js saveConfig 同时写 `imageModel` + `imageModel_{provider}` 双键 ②ui.js toggleImageProviderFields 独立键为 null 时保留 initializeConfig 已恢复的值(不覆盖) ③image-gen.js `generateImageOpenRouter`/`_gptImageI2I` 优先读 `imageModel_openrouter`(与 custom/openai 路径一致); index.html 3 个 `?v=` bump 1785926000
- **2026-08-05**: 🛡️ **切换提供商不再刷新模型列表 — 仅保存时刷新** — 根因: `onProviderChange` (utils.js) 切换提供商后 200ms 调 `fetchModels(true)` 刷新模型列表 + `saveConfig` (config.js) 保存后 1500ms 再刷一遍 → 双刷新, 切换过程中断正在生成的模型; 修复: 删除 `onProviderChange` 内的 `fetchModels` 调用, 模型列表刷新统一在 `saveConfig` 时执行, `fetchModels` 入口的 `_activeStreamChatId` 守卫保留防保存时中断; utils.js?v= bump 1785925001
- **2026-08-05**: 🧠 **自定义模型思考强度支持修复** — 根因: 通配符配置 `match:['*']` (models.js) 的 `supports` 缺 `REASON_EFFORT`/`THINKING_LEVEL` + `bannedParams` 含 `reasoning_effort` → 自定义模型回落通配后思考强度 UI 被 `supportsThidingIntensity()=false` 隐藏, 且请求参数被 sanitizeBody 删除; 修复: 通配 supports 加 `S.REASON_EFFORT`/`S.THINKING_LEVEL`, bannedParams 移除 `reasoning_effort`; getThinkingIntensityParams 兜底走 OpenAI 兼容 `reasoning_effort` 路径; index.html models.js?v= bump 1785925000
- **2026-08-05**: 🛡️ **配置面板切换提供商中断流式输出修复 (Triple Guard)** — 根因: `onProviderChange` (utils.js) 切换提供商时立即改 `modelSelect.value` → 触发 select change 事件 → `saveConfig` → `fetchModels` → 替换 `modelSelect.innerHTML` → 中断正在流式输出的模型; 修复: 新增 `window._activeStreamChatId` 全局标记 (main.js:829 设置, main.js:450/4323 清除); `fetchModels` (config.js:1204) 入口守卫, 有活动流则跳过刷新; `onProviderChange` (utils.js:55-60) 有活动流时仅更新 localStorage 不改 DOM `.value`; 3 文件改动全过 node --check
- **2026-08-05**: 🎨 **AI 生成图片画布增强 Phase 1 (悬停操作栏 + 灯箱升级 + 元数据传播)** — 气泡内生成图片新增悬停操作栏(下载/复制/以图生图/重新生成); 灯箱升级(顶部元信息条显示提示词/模型/时间 + 底部操作栏); generatedImages 存储格式升级为新旧兼容的对象数组 {url, prompt, model, aspect_ratio, timestamp}; 新增辅助函数 getImageUrl/getImageMeta/buildImageMeta; 全部 UI 控件使用 SVG 图标替代 emoji; 4 文件改动 + CSS 新增 .img-action-bar/.lightbox-info-bar 等样式; index.html `?v=` bump 1785911000
- **2026-08-05**: 🧠 **思考强度分级系统 (off~ultra) — 统一 6 档 + 全提供商映射** — 学习主流模型原生分级: OpenAI reasoning_effort (minimal/low/medium/high) / Claude output_config.effort (low/medium/high/xhigh/max) / Gemini thinking_level / DeepSeek reasoning_effort (low/medium/high/max); 统一 6 档 (off/low/medium/high/max/ultra) 全局生效 (普通聊天+Agent 模式); 新增 models.js THINKING_INTENSITY_MAP + getThinkingIntensityParams() 按模型自动映射; ultra 选项仅 Claude/DeepSeek V4 显示; 替换 agent-only 的 agentThinkingDepth (3 级) 和 LongCat/MiniMax 二元开关; 旧配置自动迁移 (shallow→low/standard→medium/deep→high, disabled→off); /effort 命令升级为 6 档 + 同步 UI 选单; 18 项映射单测全过 + 6 文件 node --check
- **2026-08-05**: 🛡️ **切换提供商不再打断正在输出的模型** — 根因: `onProviderChange()` (utils.js) 切换提供商时自动 setTimeout 200ms 调用 `fetchModels(true)` 刷新列表, fetchModels 将 modelSelect innerHTML 重置为"加载中..."再替换 → 打断正在生成的模型; 双修复: ①utils.js onProviderChange 检测 `.bubble.streaming/.typing/.gen-active` 有活跃输出时跳过自动刷新 (无输出时保持原行为) ②config.js fetchModels 入口加活跃输出守卫 (有输出直接 return + toast 提示); index.html 2 个 `?v=` bump 1785906001
- **2026-08-05**: 🔧 **自定义导入 GPT 模型 max_tokens 锁 4096 修复** — 根因: models.js 通配符 fallback 配置 (match:['*']) 硬编码 maxOutputTokens:4096,自定义模型名不含已知前缀(gpt-4o/gpt-4-/...)时落回此通配 → config.js 输入框上限钳制 4096 + getMaxOutputTokens Math.min 双重压制 (即使 /models 返回真实上限也不生效); 修复: 通配符 maxOutputTokens 4096→131072 (与 contextWindow 一致), defaultMaxTokens 保持 4096 默认发送值不变, AutoAdjust 学习值会在真实上限更低时自动收紧; index.html models.js?v= bump 1785922961
- **2026-08-05**: ⌨️ **斜杠命令扩充 + @ 提及 + 文件夹上传 (Phase 4)** — 命令列表从 21→29 个: 新增 /mcp(MCP工具状态) /cost(费用统计) /effort(推理力度 low/medium/high/max) /think(深度思考开关) /queue(消息队列) /attach(上传文件) /folder(上传文件夹) /theme(别名 /color) /abort(别名 /stop); @ 提及系统: 输入 @ 弹出子代理/已附着文件/工具候选列表 (↑↓导航 Enter确认 Esc关闭), 复用 slash popup 样式, 候选源包括 _cachedAgentList + _agentListCache + pendingFiles + 常用工具; 文件夹上传: index.html 新增 #folderInput (webkitdirectory), init.js 监听 change 调 processSelectedFiles; 新增 8 个 SVG 图标 (mcp/cost/effort/think/queue/theme/attach/folder); config.js 新增 _applyReasonEffort/_toggleThinking 助手 + reasonEffort/enableThinking 持久化; 4 JS + 1 HTML + 1 CSS 全过 node --check, CSS 2000/2000 平衡
- **2026-08-05**: 🎨 **图片生成提供商扩展 (OpenAI + 自定义)** — 原仅 MiniMax/OpenRouter 两个提供商,新增 OpenAI 原生生图 (gpt-image-1/DALL-E, 端点 /v1/images/generations) 和自定义提供商 (OpenAI 兼容格式)。每个提供商独立 Key/URL 字段 + 模型按提供商独立存储(imageModel_{provider});新增 _openaiImageEdit 支持 OpenAI/自定义原生图生图 (/v1/images/edits multipart form),失败降级文生图;ui.js toggleImageProviderFields 重构为 4 提供商字段映射模式;aspect_ratio→OpenAI size 映射 (1:1→1024x1024/16:9→1792x1024/9:16→1024x1792);_extractImagesFromResponse 复用解析 OpenAI data.data[{b64_json}] 格式;6 文件改动全过 node --check
- **2026-08-05**: ☁️ **云盘账号同步主项目邮箱+密码 + 切账号自动切换 + 串号修复** — 根因4点:①云盘用桥接账号{uid}@oneapichat.local而非真实邮箱;②cr_getAccessToken的glob回退跨用户捡token(串号根源);③切账号无云盘同步动作;④主项目只存password_hash云盘拿不到明文。修复:auth.php登录/注册时缓存明文邮箱+密码到userId专属文件;cr_ensureAccount优先用真实邮箱+密码登录云盘(不存在则自动注册);cr_getAccessToken删除跨用户glob回退(userId文件找不到返回空);check_login无token时自动cr_ensureAccount同步;前端切账号后调auto_login。验证:10项单测全过+实机root账号main_sync登录/新邮箱main_sync_registered自动注册/用户B空文件隔离全过;index.html 41个?v= bump 1785905928
- **2026-08-05**: 🌐 **CLIProxyAPI 部署 + 阿里云反向代理 (gpt.naujtrats.xyz)** — GitHub 46k★ 的多平台 AI API 代理(Go),以本机已有 `~/.codex/auth.json`(ChatGPT Plus, xyq070519@gmail.com) 转格式导入绕过 Cloudflare OAuth 地区封锁; Docker 部署 v7.2.118 于 `0.0.0.0:8317`; 阿里云 nginx 通过 ZeroTier 内网(192.168.195.176→192.168.195.213:8317) 反向代理; Cloudflare DNS gpt.naujtrats.xyz→115.29.211.17; Let's Encrypt 证书(至 2026-11-02); 验证: 端到端 gpt-5.4 非流式+流式 SSE 全通; 全文见 CHANGELOG
- **2026-08-03**: 🪟 **蕾米小窗 v3.1 (玻璃真透明 + 等比缩放 + 更小下限)** — 用户三点反馈修复: ①**悬停变纯色根因**: 玻璃背景 `rgba(255,255,255,0.92)` 透明度 92% 近乎不透明 — 在平色内容上 `backdrop-filter` 无内容可糊 → 看着就是纯色; 修复: 透明度降到 0.4 (暗色 0.45), `blur(16px) saturate(1.5)` + 半透明白边框 — 透见后方模糊内容才是玻璃质感; ②**等比缩放**: 原宽高独立缩放会拉变形, 改以移动量更大的轴为基准算统一缩放比 (scale 同时满足两轴 min/max 互锁), 背景卡片/圆形头像/整体形状随缩放保持一致 (CDP 实测 260×260→400×400 双轴等量); ③**最小尺寸缩小**: 150×170→96×112 (桌面) + 移动端默认 200→170px、下限 140×150→84×96 — 可缩到小头像级别; 验证: Node 12 组更新 (等比数学 220×240+dx80→300×327 / 钳制 420×458·103×112 / 玻璃 0.4+blur(16px) saturate(1.5) 断言 / 移动端 84×96+170px 断言) 全过 + CDP 20 项全过 + 7 测试文件绿; index.html 41 个 `?v=` bump 1785764276; 全文见 CHANGELOG
- **2026-08-03**: 🪟 **蕾米小窗 v3 (玻璃背景仅悬停/触摸浮现)** — 用户"背景隐藏, 只有鼠标悬停或者手指触摸时才显示透明玻璃状背景": 默认全透明 (只留圆形 GIF 悬浮, 无背景/无 blur/无边框/无阴影), `.remi-zoom-window:hover/:active` 才浮现玻璃卡片 (亮 rgba(255,255,255,0.92)+blur(14px)+边框+阴影 / 暗 rgba(17,24,39,0.92)), 标题/关闭按钮/缩放手柄 `opacity:0→1` 随玻璃一起浮现, 0.25s 过渡; 拖动/缩放进行中 :active 自然保持玻璃显示; 验证: 新增 tests/remi_zoom_window.test.js T12 源码断言组 (静止透明/无模糊/无阴影/hover 玻璃/暗色同条件/标题手柄随动) + tests/remi_cdp_verify.py 真实鼠标 Input.dispatchMouseEvent 7 项 (静止 transparent+blur:none+标题 0 → 悬停 rgba(255,255,255,0.92)+blur(14px)+标题 1 → 移开恢复, 未登录遮罩先隐藏防挡 hit-test) 全过 + 既有 11 组与 7 测试文件绿; index.html 41 个 `?v=` bump 1785763869; 全文见 CHANGELOG
- **2026-08-03**: 🪟 **蕾米放大小窗 v2 (可缩放 + 稳定性加固)** — 用户"自主调整大小且更加稳定": ①**可缩放**: 右下角新增 `.remi-zoom-resize` 缩放手柄 (nwse-resize 光标 + 双线 grip, 指针捕获拖动, 宽高独立 150-420×170-560 钳制, 缩放中 `remi-zoom-resizing` 类投影高亮); 窗口改 flex 列布局 + 图片流体缩放 (`max-width/max-height:100%` 保持圆形, 移除固定 190px/168px 尺寸), 添加 `draggable="false"` 防原生拖拽幽灵; ②**稳定性 (真实根因 — 动画重启)**: 原 `remi-zoom-dragging` 类带 `animation:none` — 移除类时 `remi-zoom-in` 动画**重新播放**, 拖动/缩放期间 `getBoundingClientRect` 读到 0.85x 缩放坐标 → 结束钳制数学全错 (窗口松手后仍在屏外)、小数位置持久化 (39.5px 抖动) — CDP 实机抓到的根因; 修复: 交互类去掉 `animation:none` (动画 0.28s 早已播完, 只换投影), 钳制写入加 `Math.round`; ③**大小持久化**: `remiZoomSize` localStorage, 打开时恢复 (越界值钳制) — 与 `remiZoomPos` 并列; ④**视口钳制**: 拖动/缩放中允许部分在屏外但至少留 56px 可抓回 (`clampZoomToViewport(true)`), 松开/打开/视口变化后整体拉回可视区 (`false`); `window.resize` + `visualViewport.resize` rAF 去抖监听 — 键盘弹起/旋转/窗口缩放自动收尺寸收位置 (自动收缩不落盘, 仅用户主动缩放持久化); ⑤**`[hidden]` 守卫**: 窗口加 `display:flex` 后 UA `[hidden]{display:none}` 被压过 → 补 `.remi-zoom-window[hidden]{display:none!important}`, 否则 JS `win.hidden` 开关完全失效; ⑥**缩放锚定**: pointerdown 先补显式 left/top 再清 `bottom` (防止 bottom 定位清除后窗口跳位); 验证: 新增 tests/remi_zoom_window.test.js 11 组 (vm 沙箱 FakeEl: 缩放数学/上下限钳制/持久化/打开恢复/视口位置钳制/尺寸收缩/拖动回归/Esc 关闭/CSS hidden 守卫/手柄样式/HTML 结构) 全 PASS + 新增 tests/remi_cdp_verify.py 实机 CDP 13 项全过 (含真实 PointerEvent 缩放拖动、rAF 等待、动画等待) + 既有 7 测试文件保持绿 + node --check; 线上 curl 三文件新内容已发布; index.html 41 个 `?v=` bump 1785763308; 全文见 CHANGELOG
- **2026-08-03**: 🔤 **字体体系自托管优化 (Inter + JetBrains Mono 实载 + 代码块等宽化)** — 用户分享「AI 流式生成字体选择」方案要求优化; 核心问题: 全站 `'Inter'`/`'JetBrains Mono'`/`'SF Mono'` 字体栈**声明了但从未加载** (无 @font-face/外链/本地文件 → 静默 fallback 系统默认), 且**聊天 markdown 代码块无 font-family** (流式输出代码用比例字体渲染, 逐字宽幅抖动/缩进不稳); 修复: ①**自托管字体**: jsDelivr (fontsource) 下载 Inter + JetBrains Mono 各 4 字重 latin woff2 → `public/fonts/` (8 文件 ~180KB; 站点面向国内 Google Fonts 被墙, 自托管零外链; 中文仍走系统栈 PingFang/雅黑/Noto — 文章「中文用系统字体」建议); ②**@font-face ×8**: `font-display:swap` (先渲染 fallback 不阻塞首屏) + `unicode-range` latin 子集限定; ③**:root 统一字体变量**: `--font-sans` (Inter 自托管 + 中文系统栈) / `--font-mono` (`JetBrains Mono` 领先 + Fira Code/Cascadia Code/Source Code Pro/Consolas/Monaco 兜底, 文章代码推荐栈), body 改 `var(--font-sans)`; ④**代码块等宽化 (流式稳定性核心)**: `.markdown-body pre/code` 补 `var(--font-mono)` — 逐字输出字符宽固定布局不跳动; ⑤**10 处散落字体栈统一**: 文件预览/slash 命令/日志查看器/审批标签/工具状态行/密钥显示/技能 textarea/diff 视图全改变量 (原 SF Mono 领先的 macOS 专用栈统一为 JetBrains Mono 领先); 验证: woff2 魔数 8/8、CSS 括号平衡 (1996/1996)、3 JS node --check、全量 grep 无残留裸 monospace/游离 Inter、线上 curl fonts 200 + style.css 含 @font-face 与 --font-mono 已发布; index.html 41 个 `?v=` bump 1785763308; 全文见 CHANGELOG
- **2026-08-03**: ⚖️ **转场遮罩节奏修正 (切入提速 + 切出遮住切换过程)** — 用户反馈上一轮遮罩等待方案「切入慢了切出快了」: ①切入慢根因: 进入遮罩 maxWait 3500ms 且 minWait 900ms — 大会话渲染(数百条消息 DOM 构建)常超 1s, 遮罩滞留感明显; ②切出快根因: 退出遮罩**近乎透明** (`rgba(0,0,0,0.15)`+6px 模糊) — 恢复聊天仍等 750ms 才 loadChat, 期间用户透过半透明遮罩**清楚看见**侧边栏展开显示 Agent 会话列表 + 聊天区仍是 Agent 会话, 然后遮罩才淡出, 视觉上「遮罩消失时内容还没切换」; ③修复: ①退出遮罩改**高不透明度深色实底** `rgba(15,23,42,0.88)`+blur(10px) brightness(0.6) — 切换全程在遮罩后完成, 任何中间状态不可见; ②退出恢复聊天从 750ms 提前到 **120ms** 立即开始 (遮罩已不透明, 无需等动画播完) — 内容切换与遮罩动画并行, 淡出时必然已切换完成 (minWait 500/maxWait 2000); ③进入遮罩 minWait 900→**750ms** (动画实际 0.7s 完成), maxWait 3500→**1800ms** 兜底 — 加载快时更快淡出, 加载再久也不滞留; ④临时授权动效 900/2500→700/1800; ⑤**顺带修复潜在根因**: WS 流结束 (agent-notify.js done/error) 只清内存变量不清 localStorage — `_wsStreamId`/`_wsChunkCount` 陈旧残留导致 loadChat 的 WS 续接块 (dialogs.js:839) 误触发: 最长等 3s WS 连接 (切入慢) + 发送陈旧 resume 后**早退不渲染** (切出后内容不切换) → done/error 补 `localStorage.removeItem` 双键; 验证: tests/agent_chat_separation.test.js `testTransitionOverlayWaitsForLoad` 断言更新 (750/1800 + 500/2000 + 遮罩不透明度 + 120ms 立即切换 + WS 残留清理) 全 PASS, 既有 7 测试绿, node --check 3 文件通过, 线上 curl 确认已发布; index.html 41 个 `?v=` bump 1785763050; 全文见 CHANGELOG
- **2026-08-03**: 🎬 **Agent 模式转场遮罩等待底层加载完成再淡出** — 用户反馈遮罩在后方聊天/元素加载完前就消失, 视觉割裂: 根因: 进入/退出特效 (`playAgentEnterEffect`/`playAgentExitEffect`) 用**固定计时器**自动淡出 (进入 900ms/退出 650ms), 而底层 `loadChat` 是 async — 大会话渲染可能超过固定时间, 退出场景更糟: 恢复聊天 `setTimeout(loadChat, 750)` 在遮罩消失 (850ms) **之后**才开始加载; 修复: ①新增 `_dismissOverlayAfter(key, loadPromise, minWait, maxWait)` 遮罩淡出辅助 — 同时满足「最短展示时间 (动画完整播完)」与「底层加载完成 (loadChat promise resolve + 150ms 渲染稳定帧)」才淡出, `maxWait` 兜底防加载异常卡死遮罩, 元素引用比对防快速切换模式时误淡新遮罩; ②两个特效函数删除固定自动淡出计时器 (存 `timer: null`, 清除逻辑兼容); ③setAgentMode 接线: 进入 agent/yolo 捕获 `_enterLoad` (createAgentChat→loadChat 链) 完成后淡出; 退出 off 在 750ms 恢复 loadChat 完成后淡出; plan 无聊天切换按固定 900ms 淡出; 无恢复聊天按 850ms 淡出; ④tools-exec.js 临时授权动效调用点 (无聊天切换) 补 `_dismissOverlayAfter('agent', null, 900, 2500)`; 验证: tests/agent_chat_separation.test.js 新增 `testTransitionOverlayWaitsForLoad` 源码断言组 (辅助函数存在/两特效无固定计时器/enter/exit/plan 接线/兜底超时/临时授权调用点) 全 PASS, 既有 6 测试绿, node --check 2 文件通过, 线上 curl 确认已发布; index.html 41 个 `?v=` bump 1785762391; 全文见 CHANGELOG
- **2026-08-03**: 🖱️ **Agent 模式侧边栏手动展开修复 (三重解禁)** — 用户反馈 Agent 模式侧边栏点击无效: 根因三层叠加: ①CSS `.agent-active #sidebarToggle { pointer-events:none }` (按钮置灰死点击, 点击事件根本到不了 JS); ②`toggleSidebar` (ui.js) 的 `isAgentToolsActive()` 提前 return + toast「Agent 模式下侧边栏已折叠」; ③`updateAgentUI` (agent.js) 每次调用 (心跳/SSE/通知/模式切换频繁触发) 都强制重新收起侧边栏 — 即使用户手动展开也会被下一次状态刷新打回; 修复: ①删除 CSS 禁用规则 (按钮恢复全交互); ②`toggleSidebar` 移除 Agent 模式提前 return (移动端抽屉/桌面端 collapsed 切换通用); ③`updateAgentUI` 侧边栏收起/展开改为**模式切换门控** (新增 `_lastSidebarSyncMode` 标记, 仅模式变化时强制执行一次, 状态刷新不再覆盖用户手动展开的状态); 进入 Agent 模式仍自动收起 (setAgentMode + init.js 启动路径不变), 展开后可见 Agent 会话历史 (上轮严格分隔已保证只显示 Agent 会话); 验证: tests/agent_chat_separation.test.js 新增 `testSidebarManualExpand` 源码断言组 (ui.js 无禁止分支 + agent.js 有门控 + style.css 无禁用规则) 全 PASS, 既有 6 测试绿, node --check 3 文件通过, 线上 curl 确认三文件新内容已发布; index.html 41 个 `?v=` bump 1785761719; 全文见 CHANGELOG
- **2026-08-03**: 🔀 **Agent 会话与普通聊天严格分隔 + Agent 模式历史记录功能** — 串域根因 5 类 12 处: ①历史列表泄漏 (`renderChatHistory` 只过滤 `_agent_main`, 归档 `_agent_old_*` 全进普通列表 + 空列表兜底显示 `_agent_main`); ②`/new` 静默覆盖 (归档条件要求 `currentChatId === '_agent_main'`, 其他聊天时按 /new 旧会话永久丢失); ③恢复/兜底 9 处串域 (lastChatId/fallback/清除循环/退出恢复候选/init.js `createAgentChat` 无条件覆盖 `_agent_main`/deleteChat 兜底/SSE chat:deleted/ensureChatExists/cleanupOldChats NaN 排序先删); ④任务回复串域 (`_triggerMainAgentForTask` 强切聊天、`_pendingAgentReply` 任意 finally 消费发进 currentChatId、`_doTrigger` 无主结果绑当前聊天); ⑤`_agent_main` 删除墓碑陷阱; 修复: 公共助手 `window.isAgentChat(id)` (25 处统一), renderChatHistory 按 `isAgentToolsActive()` 严格分隔 (Agent 视图=Agent 会话 `_agent_main`+`_agent_old_*` 可点击加载, 普通视图=普通聊天, plan 保持普通视图), `/new` 归档条件放宽 + 清墓碑, 9 处恢复/兜底按域过滤, 任务回复同域才切换/发送 (`_pendingAgentReplyChatId` 双端校验, `_doTrigger` 临时授权保活其余绑 `_agent_main`), `_agent_main` 不渲染删除按钮; 新增 `tests/agent_chat_separation.test.js` 8 组真实源码提取用例全 PASS + 既有 5 测试绿 + node --check 9 文件; index.html 41 个 `?v=` bump 1785761294; 全文见 CHANGELOG
- **2026-08-03**: 🔍 **模型死循环检测机制 (双端 LoopGuard)** — 小参数模型易陷入工具复读/振荡/文本复读死循环烧 token (真实案例 76 个相同 video_download); 新增 `public/js/loop-guard.js` (纯 JS 可测) 6 检测器: 工具重复 ≥3 次(soft)/工具振荡 ABABAB(soft)/连续纯工具轮 ≥6 轮(hard)/正文复读(周期检测 8~100 字符 97% 同相位, 模板文本 95% 不误报)/推理死循环(正文为 0 + 复读, 8000 字符兜底)/无进展(8字符块唯一率 <30%); **分级处理**: soft = 不执行重复调用 + 注入【系统提示】引导模型收敛 (复用 _oneShotTool 注入管线保 tool_call_id 配对), soft ≥3 次升级 hard = 中止请求 + `.error-detail` 详情 (原因/模型名/耗时/tokens/建议); 接线: main.js 会话级 guard (`__loopGuardMap[chatId]`, finally 释放) + `_throwGuardHard` + 2657 catch 前置 Guard 判定 (防误判网络错误走非流式降级) + 4155 catch 最前分发; stream-handler.js `_guardFeed` 6 处埋点 (SSE/流式/非流式正文+推理); 引擎端 `python/engine/loop_guard.py` 同策略集成子代理 `_run` (skip → 注入+强制总结轮, abort → 终止, agent:warning 广播) + workflow.py; 配置 UI 3 项 (开关/重复阈值/纯工具轮阈值, Agent 设置区); 测试: Node 12 用例 + Python 15 项 + 回归全过 + CDP 实机检测器验证全过; index.html 41 个 `?v=` bump 1785760178; 全文见 CHANGELOG
- **2026-08-03**: 🔧 **刷课 AI 答题配置同步修复 + 联网搜索 (ai_sync 服务器配置源)** — 根因: ①`syncAIKey()` 只读 `localStorage.apiKey/baseUrl` (主客户端旧单 key 存储) — 服务器端多配置/跨设备更新后 localStorage 过时 → 同步到过时配置; ②`syncModels()` 用过时 baseUrl 拉 `/models`, fallback 硬编码 `deepseek-chat` → 只显示两个过时模型; ③AI 答题无联网搜索; 修复: ①新增 `chaoxing_api.php?action=ai_sync`: 数据源 **SQLite `user_config` 表** (主客户端 chat.php save_config 实际存储; 首次实现误读 `users/{uid}_config.json` 文件导致同步无效, 已修正) + `decrypt_config_key()` 解密 (apiKey 按 baseUrlProvider 取对应加密键, 搜索 key 按 searchProvider 选 tavily/brave/通用) → 返回 base_url/api_key/model/search_key/search_enabled; ②前端 `syncAIKey()`/`loadTiku()`/`syncModels()` 优先服务器 ai_sync (localStorage 仅兜底), 模型列表用同步后的 baseUrl+key 拉取; ③刷课配置新增 `ai_search`/`ai_search_key` 字段 (save_tiku/tiku_config 透传, 表单加开关+Key 输入); ④answer.py AI._query 联网搜索增强: ai_search=1 时先 Tavily 搜题 → 搜索结果摘要拼入 prompt → AI 回答, 搜索失败降级不影响答题; 验证: mock 全链路 (言溪失败→Tavily 搜索→AI 返回 A); PHP v2 加密值解密还原; save/tiku_config 新字段透传; 前端 JS 语法 OK; 验证: root 账号 SQLite 配置同步出真实 DeepSeek key + Tavily key + deepseek-v4-flash; 测试后配置已恢复备份
- **2026-08-03**: 🛠️ **八项全面修复 (粗光标根因/空占位/链接弹app/侧边栏统一/蕾米恢复/图表放大/密钥暗色/缓存多模型)** — ①**搜索后「粗彩色光标」根因**: `typing`+`streaming` 类同时命中时, typing 规则的 `width:1.5em`(25px) 泄漏给 streaming 渐变光标 (CSS 特异性 4类>3类, content 被 typing-dots 动画置空但宽高残留) → 25×17px 粗渐变块; 修复: 双规则改**互斥选择器** `:not(.streaming)` + streaming 规则显式 `width/height/content !important` 重置; ②**光标残留(刷新才消失)**: `_streamState[chatId]` 被新流覆盖/中止路径漏清 → cleanupStreamState 补删 `typing` + 完成路径**全量清扫**所有气泡三个类 (main.js 3528 处, 链式轮收尾 3449 处补删 streaming/gen-active, 402降级/工具降级/解析失败/abortExistingRequest 同步补) — DOM 只渲染当前聊天, 全局清扫安全; ③无正文气泡 → 灰色斜体 `(empty)` 占位 (原"已完成深度思考/已执行工具调用"文案按用户要求改为 (empty)); ④**PWA standalone 外链不再弹到浏览器/app**: 委托点击拦截 (display-mode: standalone 检测), http(s) 外链 `preventDefault` + 当前窗口打开 (返回键回聊天), 站内链接保持原行为; ⑤移动端侧边栏「功能」块 → 与「偏好设置」同款玻璃卡片 (0.55 白/0.035 暗 + 14px 圆角 + 渐变指示条头 + 12px 按钮 indigo 图标, 双主题); ⑥蕾米小窗刷新后自动恢复: localStorage `remiZoomOpen`/`remiZoomPos` 持久化, 加载后轮询等头像就绪自动重开 (200ms×40); ⑦**Mermaid 图表 + K线图点击放大**: 委托监听 `.mermaid svg` (SVG 序列化 data URL 进灯箱, 滚轮缩放/拖拽平移/下载) + `.markdown-body img` (整组翻页, 跳过生成图容器/链接内图片), zoom-in 光标; ⑧API 密钥区暗色: 密钥条目白底 `#f9fafb` → `.api-key-item` 暗色 0.06 白, 撤销按钮暗红变体, **「创建新密钥」按钮根因: Tailwind 裁剪集缺 `dark:bg-indigo-900/20`/`dark:text-indigo-400`** 暗色回退亮 indigo-50 → 属性选择器显式双主题 (暗 0.12 靛蓝底 + indigo-300 字, hover 0.22); ⑨**缓存命中率全模型**: `window._extractCacheHit` 统一提取 (DeepSeek prompt_cache_hit_tokens / OpenAI prompt_tokens_details.cached_tokens / Anthropic cache_read_input_tokens / Gemini promptTokensDetails[].cachedTokenCount / Grok 顶层 cached_tokens) + `_extractPromptTokens` 基数, main.js 3 处累计 + 页脚提取全部接入; 验证: CDP 实机 — typing+streaming ::after 2px×14px (原 25px), 空气泡 "(empty)" 灰, lightbox 图片/SVG 双开, 暗色 api-item 0.06/按钮 0.12+indigo-300, 移动端侧边栏与偏好卡 computed 完全一致, 蕾米刷新重开+位置恢复, 缓存 6 格式提取全对, standalone 点击 defaultPrevented; index.html 40 个 `?v=` bump 1785759325; 全文见 CHANGELOG
- **2026-08-03**: 🎨 **气泡主流化 round 3 + 面板输入框字体统一 (v4.2)** — ①记忆系统"文字位置不统一"真实根因找到: style.css 存在**重复的 `.config-input` 块** (2071 行, 旧版 rem 单位: `font-size:0.875rem`=14px/`padding:0.375rem 0.75rem`/radius 0.375rem/outline focus) 压过标准块 (1242 行 12px/8px 12px/8px) → 全面板**普通文本输入框 14px 而 select 12px** (config-select `12px !important` 不受影响) — 记忆系统键/内容输入框最显眼; 修复: 删除重复块, 全面板输入框统一 12px/8px 12px (CDP 断言 32 个 config-input 字体集 {12px, 11px}, 无 14px); ②气泡主流化 round 3: 用户气泡**去光晕投影** (主流 2026 Agent 项目全部扁平无影, 保留用户指定的渐变+白字+尾角), 气泡 padding 0.4rem 0.6rem → **0.75rem 1rem (12px 16px)** + line-height 1.65 (主流呼吸感留白, 与 injectStyles 的助手气泡 12px 16px 对齐, 双气泡 padding 完全一致); 验证: CDP — userPad=asstPad=12px 16px, userShadow=none, 字体 15.2px/行高 25px, 输入框无 14px; index.html 40 个 `?v=` bump 1785753300
- **2026-08-03**: 🐛 **重新生成改为完整重跑调用链 + 工具调用状态行移除 (v4.1)** — ①重新生成根因: `regenLastAssistant` (rendering.js) 只找**最后一条助手消息**截断 → 用户问题后的多轮调用链 (工具轮/中间助手消息) 残留历史里, 重跑时模型看到旧工具结果; 修复: 改找**最后一条用户消息**, 删除它之后全部消息 (含问题本身), 再重发该问题 → 完整调用链重新执行; 验证: 构造「旧问答 + 新问题 + 工具轮 + 工具结果 + 最终回答」→ regen 后仅保留旧问答, 问题后全链删除; ②「🔧 工具调用: web_search(...)」状态行移除: tools-exec.js web_search/web_fetch 分支的 `.search-status` 创建块删除 (实时状态已由 tool-call-lines + 顶部浮动状态栏承担); 验证: 服务端 tools-exec.js 无「工具调用/正在抓取网页」字样; index.html 40 个 `?v=` bump 1785753000
- **2026-08-03**: 🛠️ **五项 UI 修复 (搜索完成行移除/呼吸光标泄漏/空气泡美化/页脚只留最后一个/缓存命中恢复)** — ①搜索完成状态行整体移除 (main.js web_search 分支 status.remove(), 进度由顶部状态栏+滚动标题条承担); ②呼吸彩色光标 (`.bubble.streaming .markdown-body::after` 2px 紫蓝渐变呼吸竖线) 泄漏根因: `streaming` 类只加不删 (markdown.js 旧版只加不删虽有收尾但中止/切换气泡路径漏清) → 清扫逻辑加入 `streaming` 类 + 流结束/错误路径补删; ③无正文空气泡 (推理-only/工具-only 消息) → `window._ensureEmptyBubbleHint` 渲染灰色占位 (已完成深度思考/已执行工具调用), 流式结束+非流式完成双路径接入; ④页脚 (时间/token) 每个气泡都显示 → **只保留最后一个助手气泡**: appendMessage 修剪移到 `container.appendChild(row)` 之后 (气泡挂入容器前 closest 拿不到容器, 修剪永远留两个 — 根因), finalizeBubbleUI 先清其他气泡页脚再加; ⑤缓存命中率恢复显示在最后一个气泡页脚 (rendering.js 缓存段恢复, 4 格式兼容提取保留); 验证: CDP — 页脚 footCount=1 且内容含 `60.0%缓存命中`, finalize 路径旧气泡页脚清除, streaming 清扫通过, 空气泡占位文案正确; index.html 40 个 `?v=` bump 1785752700
- **2026-08-03**: 🐛 **搜索完成状态行不再注入原始结果 (气泡内)** — 用户展示实例"蕾米 ✅ 搜索完成: 【原始联网搜索结果】1. 8月4日...链接:..." → 根因: 工具调用 web_search 完成分支 (main.js:3233) 把 `resultContent.substring(0,100)` 塞进气泡内 `.search-status` 行; 修复: 改为纯文本 `✅ 搜索完成` (标题/链接已由气泡外滚动标题条展示, 状态行只是短暂确认, 答案开始输出即随 placeholderCleared 移除); 验证: 服务端 main.js 含新逻辑; index.html 40 个 `?v=` bump 1785752400
- **2026-08-03**: 🛠️ **工具测试报告全面审查修复 (股票三连 + 高德三重)** — ①K线「未找到数据」根因: 东财**废弃 lmt 参数** (一律 rc:102) → get_kline 改 beg/end 动态计算+截取; ②东财**IP 风控** (push2 302→push2delay 延时, push2his 拒连): `_http_get` 加 push2delay 兜底 + **腾讯行情 fallback** (fqkline 6 字段参数/日K第7元素dict防御, 分钟 mkline, 实时 qt.gtimg.cn) + Retry connect=0 快速失败; ③sector_flow 小单字段误复制 f62→f70; ④indicators 500: KDJ 除零 ±inf (pd.notna 不识别) + float64 存 None 转回 NaN → to_dict 后逐值 isfinite→None; ⑤PROJECT_ROOT parents[2] (图表存 python/uploads 目录 URL 404) + uploads/stock_charts 权限; ⑥高德: driving/walking/bicycling 取错字段 `routes`→`route`/`data` (永远空) + summary 摘要; schema_personal_map **JSON body 读取** (PHP $_POST 不吃 application/json) + **poiId 自动补全** (WIA 必填) + foreach `?? []` 引用拷贝陷阱; ip_location 空数组友好提示; ⑦运维: 引擎是 **PM2 管理** (非 systemd), 手工 nohup 抢端口致 PM2 重启循环 69 次; 验证: 引擎端点 8/8 (腾讯源/无NaN/图表200/板块30), unittest 25 OK; 全文见 CHANGELOG
- **2026-08-03**: 🐛 **缓存命中率从气泡页脚移除 (只留会话总结)** — 用户"缓存命中率那一行能不能在最后总结显示, 不要每个气泡都显示一次": 根因: `_buildMsgFooterHtml` (rendering.js) 把耗时/token/缓存命中全塞进每个助手气泡的页脚 (usage.prompt_cache_hit_tokens 等兼容 4 种格式提取); 修复: 页脚只保留耗时+token, 缓存命中段移除 (会话级用量面板 `usagePanel.cacheHint()` 已有「💾 缓存命中率 X% (H/M)」, 数据由 sessionUsage 累计); 验证: `_buildMsgFooterHtml({prompt_cache_hit_tokens:80,...})` → footer 无"缓存"字样, 1.5s+150 tokens 保留; index.html 40 个 `?v=` bump 1785752100
- **2026-08-03**: 🎛️ **工具状态行保留 + 残留总结清除 (v3.9)** — 用户澄清: 实时逐行工具状态 (转圈/勾/叉 + 工具名 + 参数) **要保留**, 只去掉工具执行完后的残留总结; 调整: ①CSS 隐藏规则只保留 `.tool-calls-history` (历史「N 次工具调用」折叠块), `.tool-call-lines` 恢复显示; ②stream-handler.js 两处答案开始输出点清除残留行: 流式首批内容到达 (placeholderCleared 块) + 非流式 handleNonStream 开头 — 工具全部执行完、答案开始生成瞬间, 气泡内完成行列表整体移除 (执行期间逐行状态全程可见); 验证: CDP — history display:none, live lines display:block; index.html 40 个 `?v=` bump 1785751800
- **2026-08-03**: 📏 **Cloudreve 存储限额调整 (25G/150G)** — groups 表 `max_storage` (字节): User 组 15G→25G (26843545600), Admin 组 15G→150G (161061273600); 主账号 xyq070519@gmail.com (id=13) 从 User 组移入 Admin 组 (group_users=1, 原已用 13.6G 超 15G 风险解除, 获得管理面板权限); 其余 9 用户 (含桥接账号) 保持 User 组 25G; 直接改 `/opt/cloudreve/data/cloudreve.db` (v4 表结构: `groups.max_storage` + `users.group_users`), user_info API 实测即时生效无缓存
- **2026-08-03**: 🎛️ **气泡内工具调用信息总结块隐藏 (v3.8)** — 用户"同时隐藏气泡内那个工具调用信息总结块": ①历史渲染: loadChat 对带 tool_calls 的助手消息渲染 `<details class="tool-calls-history">`「N 次工具调用」折叠块 (dialogs.js) — 在气泡内; ②实时渲染: `showToolStatus` (ui.js) 在气泡内建 `.tool-call-lines` 容器逐行显示工具名/参数/状态 (spinner/勾/叉) — 也在气泡内; 修复: **纯 CSS** `.tool-calls-history, .tool-call-lines { display:none !important }` — 两处总结块全隐藏, 工具调用信息仍由气泡外工具卡片 (appendToolCallMessage 独立行) + 顶部浮动状态栏提供, 零 JS 改动; 验证: CDP — 历史 details 存在且 display:none, 注入 tool-call-lines 同样 none, 工具卡片创建正常不受影响; index.html 40 个 `?v=` bump 1785751500 (顺带归一化一个并行写入的杂散版本号 1785753272)
- **2026-08-03**: 🗂️ **Cloudreve 云盘全面结合 (上传/下载/生成全链路自动入云盘)** — 镜像同步架构 (本地 uploads/ 与 URL 不动, 聊天历史零回归; 直插 Cloudreve v4 DB 零拷贝因 schema 复杂+升级风险放弃); 新增 `api/cloudreve_lib.php` 公共库 (curl 助手/token 管理/分片上传抽取 + `cr_probe/cr_ensureAccount/cr_ensureFolder/cr_importFile`); 账号三级解析: 绑定凭据→邮箱匹配(如 root=主账号 13)→桥接账号 `{uid}@oneapichat.local` 自动创建; 云盘目录: 每用户 `OneAPIChat/{uploads,downloads,generated}`; `import_file` action (cr_shared+user_id 内部通道); 接入点: upload.php / netdisk_api.php / engine_server.py (`_import_to_cloudreve` ⚠️禁代理防 Mihomo SOCKS5 SSL EOF) / video-hunter-bridge + download-monitor + bilibili-bridge / server.js mmx 音频 / engine_api+call.php 透传 user_id; 顺带修复 netdisk_api.php sessions.json 认证结构 bug (key=token); 存量迁移脚本 `api/cloudreve_migrate.php`; 验证: 上传/下载/docx/视频 4 链路实机全通落库 owner 正确
- **2026-08-03**: 🎛️ **显示设置新增「隐藏推理过程」开关 (v3.7)** — 用户"看一堆推理过程块太累" → 显示设置加开关 `#hideReasoningToggle` (config-toggle-row): `window.toggleHideReasoning(checked)` (config.js) 持久化 `localStorage.hideReasoning` + 切换 `body.hide-reasoning` 类, init.js 启动恢复; 隐藏机制: **纯 CSS** `body.hide-reasoning .reasoning-details { display:none !important }` — 已渲染/流式中/历史加载的推理块全部即时隐藏 (零 JS 渲染改动); 思考状态体现: stream-handler.js 新增 `_ensureReasoningChip(bubble)` — 隐藏模式下流式收到首个推理 chunk 时在气泡内追加「💡 思考中」小指示条 (6px 紫色脉冲圆点 + 文字, 双主题配色, prefers-reduced-motion 停用), 流结束清理时移除; 4 个推理块创建点 (SSE handler ×2 + streamResponse chunk ×2) 统一接入; 验证: CDP — 开关在显示设置内 ✓, toggle on → body 类 + 已加载推理块 display:none + stored '1', 流式 chip 生成 (思考中 + reasoning-pulse 动画), toggle off → 恢复 block; index.html 40 个 `?v=` bump 1785751200
- **2026-08-03**: 🎨 **用户气泡恢复渐变版 (v3.6)** — 用户反馈"之前挺好的", 指定恢复 v3.3 渐变版: 用户气泡从 ChatGPT 灰 (#e9e9eb) 改回 **indigo→蓝渐变** `linear-gradient(135deg,#6366f1,#3b82f6)` + 白字 + 尾角 0.35rem + 微光投影 `0 2px 10px rgba(79,70,229,0.22)` (同特异性后置覆盖亮色加固 `#2563eb!important`); 助手气泡同版恢复轻表面 (亮 `rgba(255,255,255,0.6)`/暗 `rgba(255,255,255,0.04)`, 无边框无阴影); 验证: CDP 双主题 — 亮 user 渐变+白字+19.2/5.6px 圆角+投影, 助手 0.6 无影; 暗 user 渐变不变, 助手 0.04; index.html 40 个 `?v=` bump 1785750800
- **2026-08-03**: 🎨 **气泡主流化 round 2 (ChatGPT 灰 + Claude 透明) + 记忆系统 emoji 清理 (v3.5)** — 用户两次要求"参考主流 Agent 项目"且此前反感渐变按钮太显眼 → ①用户气泡改 **ChatGPT 风柔和中性灰**: 亮色 `#e9e9eb` 深字/暗色 `#3a3a3d` 浅字, **对称圆角 17.6px 去尾角**, 去投影 (覆盖 v3.3 的 indigo→蓝渐变 — 同特异性后置 + `background-image:none!important` 压过加固规则的 `#2563eb!important`); ②助手气泡改 **Claude 风完全透明**: 内容直接落在页面背景上, 无边框无阴影无背景 (覆盖 v3.3 的 rgba(255,255,255,0.6) 轻表面), 代码块/工具卡自带背景不受影响; ③记忆系统"文字位置不统一"残余根因: label/按钮里的 **emoji 前缀** (🎭 人格预设/💾/🔄/🗑) — emoji 字形撑高 11px 行盒使文字基线偏移, 与面板其他纯文本 label 不一致 → 全部移除 (人格预设/保存记忆/刷新/清空); 验证: CDP 双主题 — 亮 user `rgb(233,233,235)`/深字/17.6px/无影, 暗 user `rgb(58,58,61)`/浅字, 助手双主题 `transparent`/无边框无影; 记忆 label/按钮纯文本断言通过; index.html 40 个 `?v=` bump 1785750300
- **2026-08-03**: 🐛 **呼吸光晕滞留旧气泡 + 搜索详情块改无背景滚动标题条 (v3.4)** — ①呼吸线滞留根因: `streamResponse` 全程读共享 `activeBubbleMap[chatId]` (新消息会替换 map 条目) + 结尾 `currentBubble` 是隐式全局 (依赖最后一块 chunk 赋值), 旧流被中止/ID 守卫提前 return 时漏清 → 旧气泡永远发光; 修复: ①stream-handler.js 流开始捕获 `_ownedBubble` (全函数 7 处替换, 结尾清理/错误清理/思考折叠/图片渲染全部改用自有气泡); ②sendMessage 新建气泡前**全量清扫**残留 `typing/gen-active` (任何泄漏路径 100% 兜底) + 顺带移除旧搜索滚动条; ③完成/错误路径补删 `gen-active` (main.js 3524/413); ②搜索详情块 → 无背景滚动标题条: 工具卡片里的原始搜索结果块 (绿底 pre) 太丑 → 新增 `window.showSearchTicker()` (search.js): 气泡外 26px 无背景条, 每行「序号圆徽+标题+域名」, 内容×2 无缝循环 translateY -50% 向上滚动 (每行 2.2s), 边缘 mask 渐隐, prefers-reduced-motion 停用, 点击可打开链接; 触发点: handleSearchFlow (api-messages.js) + 工具调用 web_search 分支 (tools-exec.js) 双路径; 卡片渲染特殊化 (rendering.js): web_search 卡片只显示「✅ 已获取 N 条搜索结果 · 标题已在上方滚动展示」, **完整结果仍随 toolResult 传给模型** (截断会毁掉模型回答); 验证: CDP — 滚动条插入 `.message-content-wrapper` 气泡外 (outsideBubble=true), 8 行无缝循环, 动画 search-ticker-up 8.8s, 1.5s 后 track y 654→626 上移实测; 卡片摘要断言通过; 清扫断言通过; 呼吸线清扫兜底: 新消息开始瞬间旧气泡光晕清除; index.html 40 个 `?v=` bump 1785749900
- **2026-08-03**: 🐛 **刷课答题链修复 (言溪失败不回退 AI + 占位符 key 静默)** — 根因: ①`Tiku.query()` 自身查询失败后直接 `return None`, **从不调用 `set_fallback()` 设置的 fallback 链** — 链式题库 (TikuYanxi→AI) 从实现上就是坏的, 言溪失败永远直接随机; ②`AI._query()` 对占位符 key (「你的DeepSeek API Key」) 不识别, 发起无效 401 请求且 `except: pass` 静默; ③`TikuYanxi._query()` 失败分支直接 `res_json['data']['answer']` 索引, API 失败响应缺 answer 字段会 KeyError; 修复: ①`query()` 失败后递归 `self._fallback.query(q_info)` (多级链通用); ②`AI._query()` 空 key 或含「你的」识别为未配置 → 明确日志「请在刷课设置填写 AI Key」不再发无效请求, 请求失败/异常也记录原因; ③言溪失败分支 `(res_json.get('data') or {}).get('answer','')` 防御; 验证: 模拟言溪失败(占位 token)→ 日志「言溪未命中，回退查询 AI答题」→ 占位 key 明确跳过返回 None; 注入有效 key + mock AI 200 → 正确返回 A; data 无 answer 不再 KeyError
- **2026-08-03**: 🐛 **暗色模式 select 箭头平铺修复 (config-input shorthand 重置)** — 现象: 暗色下提供商/视觉/图像等带自定义箭头的 select 被整排向下箭头铺满; 根因: `.dark .config-input { background: #374151 }` 用 **shorthand** 且位于 `.config-select` 之后 → 把 `background-repeat/size/position` 全重置为 initial (repeat/auto/0% 0%), 而 `.dark .config-select` 只补回 `background-image` → 箭头 SVG 平铺 (亮色不受影响因加固规则用 `background-color` longhand); 修复: 改 longhand `background-color: #374151` + `.dark .config-select` 补全 no-repeat/14px/right 10px (双保险); 验证: CDP computed `no-repeat/14px/calc(100% - 10px) 50%` 全过 (4 个 config-select 断言), 亮色 no-repeat 无回归; index.html 40 个 `?v=` bump 1785749600
- **2026-08-03**: 🎨 **气泡主流化 + 记忆系统面板标准化 (v3.3)** — ①气泡参考主流 Agent 项目 (Claude/ChatGPT/DeepSeek): 用户气泡 `#2563eb` 纯色 → **indigo→蓝渐变** `linear-gradient(135deg,#6366f1,#3b82f6)` + 白字 + 尾角 0.35rem + 微光投影 `0 2px 10px rgba(79,70,229,0.22)` (同特异性后置覆盖亮色加固规则 `html:not(.dark) .bubble.user{background-color:#2563eb!important}`); 助手气泡去卡片感: 亮色 `rgba(255,255,255,0.6)` 轻表面 + **去边框去阴影** (覆盖加固规则强制白底+阴影), 暗色 `#1f2937` 硬块 → `rgba(255,255,255,0.04)` 极淡表面 (Claude 暗色同款, 内容融入页面背景); ②记忆系统面板不统一根因: `.config-btn` 类**全站无定义** (仅记忆面板使用, 渲染成默认浏览器按钮) + 大量内联样式 (标题 svg 12px/间距 8px/输入 11px vs 面板标准 14px/12px/8px 12px) → 全部改标准类: 标题 svg 14px + 标准 `config-section-title` 间距, 人格预设 label 用 `config-label`, select 加 `config-select` (自定义箭头), 输入框去掉内联字体/内边距, 按钮补 `.config-btn` 定义 (药丸灰底/暗色深灰/红 danger 变体) + 记忆条目改 `.memory-item` 类 (键名 indigo 高亮/悬停红删除/细边框列表卡, agent.js refreshMemoryList 内联样式改类名); 验证: CDP 双主题气泡 computed 全过 (亮 user 渐变+白字+投影/暗同样式, 助手亮 0.6 无影/暗 0.04 无影), 记忆区 8 项断言 (svg 14/标题 12px/label 11px/select 12px+箭头/按钮 9999px 灰底/12px); 顺带排查: `transparent-bg` 类无 JS 引用属死代码无兼容风险; index.html 40 个 `?v=` bump 1785749400
- **2026-08-03**: 🎨 **侧边栏 v3.2 (手机端功能按钮点击变白修复 + 收起文字挤列 + 边缘柔和化)** — ①手机端「功能」区 (刷课/云盘/设置, 仅移动端侧栏显示) 点击变白根因: `hover:bg-gray-200` 在亮色下是近白浅灰 (CDP 实测按压态 `rgb(229,231,235)`) → 改主题色淡填充 `rgba(99,102,241,0.08)`+`#4f46e5` (暗色 0.14+`#a5b4fc`) !important 覆盖, 与侧栏交互语言统一; ②侧边栏收起 (width→0 动画) 文字挤成竖列根因: 0 宽容器内文本自动换行逐字堆叠 → `.sidebar` 加 `overflow-x:hidden` + `.sidebar-brand/.sidebar-newchat-btn` 加 `white-space:nowrap` (CDP 实测收起过渡中 brand 高度保持 25px 单行); ③边缘锐利: 亮色加固规则 `html:not(.dark) header{...border-color:#e5e7eb!important}` 强制硬边框 → 追加同特异性覆盖: header 底边/sidebar 右缘/configPanel 左缘 亮色 `rgba(148,163,184,0.22)`、暗色 `rgba(255,255,255,0.06~0.07)`, 移动端输入框药丸边框 `rgba(148,163,184,0.28)`/暗 0.10, 分隔线降对比与渐变背景自然融合; 验证: CDP 双主题按压态/边框 computed 全过 (亮 header 0.22/sb 0.22/cp 0.22, 暗 0.07/0.06/0.06, 移动 input 0.28, 按压亮 0.08+indigo/暗 0.14+indigo-300); index.html 40 个 `?v=` bump 1785749000
- **2026-08-03**: 🎨 **侧边栏 v3.1 (新建对话空心化 + 列表底部平滑淡出)** — ①新建对话按钮渐变实心太抢眼 → 改**空心**: 透明底 + 1px 细边框 (亮色 indigo 0.35/暗色 indigo-300 0.32) + 语义色文字 (亮 #4f46e5/暗 #a5b4fc), 去投影去发光, hover 淡填充 (亮 rgba(99,102,241,0.06)/暗 0.14) + 图标旋转保留; ②列表底部硬截断无过渡 → `#chatHistoryList` 加 **`mask-image: linear-gradient(to bottom, black calc(100% - 18px), transparent 100%)`** (webkit+标准双写): mask 固定在元素边框盒不占布局 (旧版 sticky 伪元素方案占 60px 布局空间是历史 bug 根源), 内容滚入末端 18px 渐隐, 透明淡出对渐变侧边栏背景通用; ③验证: CDP 像素级 — 字形在淡出区 50% 处颜色从 (85,76,230) 混到 (152,148,240) 平滑过渡, 区外全饱和; 空心按钮双主题 computed 断言全过 (transparent/0.35 边框/#4f46e5/无阴影); 测试假数据已清理; index.html 40 个 `?v=` bump 1785748605
- **2026-08-03**: 🐛 **刷课启动报错「attempt to write a readonly database」修复 (learning_records.db 权限)** — 根因: `learning_records.db` 属主 `naujtrats:naujtrats` 权限 664, 目录 755, 而刷课 main.py 由 **www-data** 运行 → 文件 other 位只有 r、目录不可建 journal → SQLite 写操作被拒; `tracker.py` 原有自修复是**死代码**: ①`sqlite3.connect()` 在只读文件上不抛错 (SQLite 惰性降级只读打开), 真正 readonly 错误出现在第一次写操作, except 分支永远抓不到; ②www-data 对非属主文件 chown/chmod 必然失败且 `except Exception: pass` 静默; 修复: ①系统级: db 改 `naujtrats:www-data` 664 + 目录 775 (双用户可写, git 操作重建文件也可能复现此问题); ②tracker.py 重写 `_ensure_writable()`: 用 CREATE/DROP 临时表触发**真实写页面**探测 (BEGIN IMMEDIATE 只取锁不写页, 测不出), 只读时自修复 (chmod 775/664, root 才 chown), 重连重测, 仍失败抛带修复命令的明确错误; 验证: www-data 正常写入 OK; 444/555 只读下 naujtrats 运行自修复生效自动恢复 664/775; www-data 非属主只读场景报明确修复指引; db_course_status --reset-start 写路径 OK
- **2026-08-03**: 🎨 **左侧侧边栏全面优化 v3 (空白/拥挤/暗色丑边/点击变白四连修)** — ①历史列表顶部 60px 空白根因: `#chatHistoryList::before/::after` sticky 60px 渐变遮罩 (亮色白色"空白"、暗色 `rgba(0,0,0,0.3)` 黑色污痕) 直接删除 → 列表紧跟新建按钮; ②间距重平衡: 品牌行 `mb-5` 不在 tailwind 裁剪集 (实测 computed 0px) 显式补 `margin-bottom:14px`, 新建对话按钮补 `margin-bottom:10px` → 上下 14/14px 对称; ③历史项移除气泡图标 (dialogs.js 删 svg), 分组统计徽章去掉 (用户不需要); ④滚动框升级: 双主题 5px 细滚动条 (webkit+scrollbar-color), 暗色 active 项内阴影改 indigo-300 0.22; ⑤点击变白加固: autofill 覆盖 (白底自动填充 `-webkit-box-shadow 1000px #374151 inset`) + 暗色 :active/:focus 强制 `#374151 !important` + tap-highlight 透明; ⑥移动端三条杠歪根因: JS `style.display='block'` (agent.js×2/init.js×3/ui.js×2) + CSS `display:block !important` 双双击碎 flex 居中 → 全部改 `inline-flex` (CDP 实测 dx=0); ⑦删除此前误加的头部新建对话按钮 (用户澄清空白在侧边栏); 验证: CDP 桌面/移动/双主题全过 (间距 14/14/0、maskGone、dx=0、暗色 summary #9ca3af); index.html 40 个 `?v=` bump 1785748000
- **2026-08-03**: 🐛 **超星刷课面板「暂无课程」修复 (登录后缓存 session 不失效)** — 根因: cookie 过期后 `api_get_courses.py` 自动重登成功并写入新 cookie, 但 `base.py` `_cached_session` 模块级缓存 session 创建后**永不复用失效**, 第二次 `get_course_list()` 仍持旧 cookie → 超星返回未登录空页 → 解析出 `[]` → 前端显示「暂无课程」; 次要: PHP 端缓存判断 `strpos($json,'"courses"')` 把 `{"courses":[]}` 也当成功缓存 5 分钟持续误导面板; 修复: ①`login()` 成功后置 `_cached_session=None` 强制重建; ②`init_session()` 每次从磁盘合并最新 cookie (覆盖扫码登录等外部更新); ③`api_get_courses.py` 登录后仍空则报明确错误而非静默空列表; ④PHP 空结果不写缓存且顺带清掉旧空缓存; 验证: 置空 cookie 模拟失效 → 自动重登 → 第二次完整返回 13 门课程; PHP HTTPS 全链路 13 门+db_status 正常; 正常路径 0 次额外登录
- **2026-08-03**: 🐛 **Cloudreve 云盘面板空列表修复 + v2 面板重写 (目录导航 + 云盘主页通道)** — 根因: 面板取 `items = fobj.files || fobj.data || []`, 但 API 真实结构是 `{success, data:{files:[...]}}` → `fobj.files`=undefined、`fobj.data`=对象非数组 → `Array.isArray` false → **永远显示 "(空目录)"**; 修复: 改取 `(fobj.data && fobj.data.files) || []`; 同步重写 `toggleCloudrevePanel` v2: 工具栏 (面包屑各级可点/⬆️上级/🔄刷新/🌐云盘主页), 文件夹点击进入 + 原始名拼路径 (编码 URI 回传会双重编码), 文件图标按扩展名分类 (🖼️/🎬/🎵/🗜️/📝/📕/📊), 底部文件数+存储策略, 未登录态保留登录表单 + 已有账号 chips + 「打开云盘主页/去注册」按钮 (web_url 由 check_login 后端返回 `https://$hostHeader`, 前端零硬编码), 深色模式全部走 CSS 变量; 验证: CDP 真实浏览器已登录面板 7 文件夹正常渲染 (含日期/面包屑/按钮), Node 模拟旧逻辑复现 bug + 新逻辑/面包屑/上级/排序全 PASS; index.html style.css?v=/cloudreve.js?v= bump 1785747131
- **2026-08-03**: 🎨 **Toast 通知 + 数据管理按钮主题联动优化 (v2)** — Toast 亮色模式显示黑底 (`.toast` 基础背景 `rgba(30,41,59,0.94)` 深色无亮色覆盖, `.dark` 仅加深) 且样式朴素; 修复: 亮色白底/暗色深底 (`rgba(17,24,39,0.92)`) 毛玻璃 + 类型色 CSS 变量 (`--toast-color`) 左指示条 + SVG 图标徽章 (勾/叉/三角/圆点, 替换文本字符) + `toast-in` 弹性滑入动画 (0.35s cubic-bezier 右滑+回弹), 暗色类型色全部提亮 (60a5fa/4ade80/f87171/fbbf24); 数据管理 6 按钮 (导出/导入/导出当前/知识库/强制刷新/恢复默认) 由 Tailwind 半透明彩色底 (暗色下 6 色堆叠突兀) 改统一 `.data-action-btn` + `.action-{blue,green,emerald,indigo,amber,red}` 语义色变量: 亮色浅灰卡片底 (`#f1f5f9`) + 语义色图标、hover 边框/文字变语义色 + 图标微放大 + active 按压回缩; 暗色统一深灰底 (`rgba(30,41,59,0.45)`) 中性色文字 + 提亮语义色图标 (60a5fa/4ade80/34d399/818cf8/fbbf24/f87171), 与面板协调不突兀; 验证: CDP 双主题像素采样 10 项断言全过 (亮色 toast 白底深字/暗色深底浅字/图标色/按钮底色/图标色), toast 结构含 SVG 图标+色条+toast-in 动画; index.html 40 个 `?v=` bump 1785742200
- **2026-08-03**: 🐛 **头像下拉菜单错位修复 (absolute 包含块劫持)** — `#oneapiUserDropdown` 挂在按钮父容器 (gap-3 flex) 下且 `position:absolute`, 容器 `position:relative` (style.css:2492) 与 header `backdrop-filter` 双双成为包含块 → `right:0` 对齐容器右边缘而非头像按钮 (左侧还有主题/刷课/云盘/设置 4 按钮 → 菜单整体右移错位); 按钮自己设的 inline `position:relative` 对菜单无效 (按钮不是菜单祖先); 修复: 菜单挂 **body 直下 + `position:fixed`** + 新增 `positionOneapiDropdown()` 视口坐标定位 (右对齐按钮/8px 间距/视口 clamp/下方空间不足向上弹), 打开时定位 + resize/scroll 重定位跟随; 验证: CDP 实测桌面 1280x800 与移动 390x844 deltaX=0/deltaY=0, 极窄 320px 完整在视口内, 滚动后仍对齐; 与 agentModePopup 同根因同方案; index.html 40 个 `?v=` bump 1785741900
- **2026-08-03**: 🐛 **exec/python 前端永远报"命令执行失败" (await 遗漏)** — 504 修复引入 `_parseEngineJson`(async)时代替 `await r.json()` 时漏写 `await`(cloudreve.js exec/python 两分支), `d` 拿到 Promise → `.ok`/`.error` 永远 undefined → 引擎实际 200 成功也被判失败, 文件类工具不受影响(仍用 await r.json()); 修复加 `await _parseEngineJson(r)` × 2 + cloudreve.js?v= 1785741669 bump; 验证: Node 模拟 await/无 await 行为差异, 引擎日志确认用户请求全 200 成功
- **2026-08-03**: 🛠️ **Server 工具修复补充 (同链路剩余四根因)** — ①`engine_server.py` `_execute_tool` 内联 `server_file_write/append` 本地分支只允许 /tmp 前缀 → 项目内写入全被拒, 改相对路径 join PROJECT_ROOT + 允许 `/tmp/` 与 `PROJECT_ROOT/` (server_file_read 同步); ②转发映射表缺 `server_file_edit`/`server_file_grep` → 子代理调用打 `GET /engine/server_file_edit` 404, 补映射 (file_edit 端点要求 POST+JSON body, 转发分支新增 file_edit 专用 POST 分支); ③`api-tools.js` 18788 侧 `server_file_append` 路由到 `file/write` 时丢 append 标志 → 覆盖写而非追加, `execEngineProxy` 加 toolName 参数, body 带 `append:true`; ④引擎 `/engine/file/write` 只从 query 读 append/atomic, 兼容 JSON body 同字段; 验证: 全链路 (engine_api.php→18788→8766) 相对路径 write/append(追加语义)/read/edit/grep/search/op(mkdir+rm)/python(cwd=项目根)/db_query 回归通过, python/tests 核心 4 项 OK
- **2026-08-03**: 🐛 **Server 类工具部分不可用修复 (三重根因)** — ①`server_tools.py` 位于 `python/engine/` 下, PROJECT_ROOT 旧写法 `parent.parent` 少一层 → 解析成 `.../python` 而非项目根, file_write/file_append/file_op/write_chunked 允许根目录全错; ②引擎进程 cwd=/home/naujtrats, 相对路径 (`tempfile/x.md` 等) 被 `Path().resolve()` 解析到 /home/naujtrats → "写入权限受限" 全被拒; 修复: `parents[2]` + `_resolve_path()` 统一按项目根解析, engine_server.py 内联 file_write/append 同步支持相对路径, server_python `cwd=PROJECT_ROOT`; ③`file_op` 用 `locals()[path]=p` 赋值陷阱 (Python 函数内不生效) → 相对路径校验永远拿原值被拒, 改 `_abs()` 内联函数; 连带: `db_query` learning_records.db 路径随 PROJECT_ROOT 修正补 `python/` 层; 排查中发现 127.0.0.1:80/443 Host 头异常空回复 (仅测试侧, 真实域名正常); pm2 重启引擎后 15 项 Server 工具全链路回归通过
- **2026-08-02**: 📱 **工具调用卡片移动端溢出 + 键盘弹起后底部空白 (双修复)** — ①工具卡片: exec 命令作为 flex 匿名文本节点无换行规则 → 不可断 token 横向溢出, 修复为包 `<span flex:1/min-width:0/overflow-wrap:anywhere/word-break:break-word>`, 参数/输出/结果 pre 统一 pre-wrap+break-all+max-width 约束; ②键盘空白主因: iOS 输入框保留焦点时收起键盘 (点完成/下滑) 不派发 visualViewport 事件 → `--vvh` 卡在键盘弹起小高度 → 底部空一截且永不恢复; 修复三层: 1s 看门狗轮询 (脏检查零开销) + `pointerup` 交互监听 + scale 守卫重构 (旧 `scale≠1` 直接 return 会永久冻结, 改缩放变化帧跳过 + 回稳定时器 force 收敛); 新增 keyboard_viewport 8 场景 + tool_card_overflow 6 场景回归测试全过; index.html 39 个 `?v=` bump 1785684092
- **2026-08-02**: ⏱️ **长命令 exec 504 网关超时修复 (三层边界)** — 根因: ①引擎 `server_tools.py` exec 超时被硬截断 `min(timeout, 300)` (工具声明 360s 实际只给 300s); ②nginx `fastcgi_read_timeout 300s` 与引擎 300s 截断双双卡在边界 → `sleep 300` 类命令 (≈305s) 响应恰好在 300s 处抵达, 时而拿到引擎超时 JSON、时而 nginx 返回 504 HTML; ③前端 cloudreve.js 无条件 `r.json()` → 504 HTML 页报 "Unexpected token '<'"; 修复: 引擎截断 300→900s、nginx fastcgi_read_timeout/send_timeout 300→900s、前端新增 `_parseEngineJson` 防御解析 (非 JSON 返回 "引擎网关错误 HTTP 504, 命令可能仍在后台执行"); 实测 310s 命令完整返回 exit_code=0; cloudreve.js cache bump
- **2026-08-02**: 🎨 **新建对话按钮恢复渐变主色 + 全量缓存版本修复** — 新建对话按钮被"原 UI 保守优化"还原为白底细紫边 (注释「克制精致」) → 用户设备 20:19 起命中新 CSS 后报"颜色没了"; 恢复史诗级 v2 渐变胶囊 (6366f1→3b82f6 + 投影 + 悬停上浮, 深浅色双主题); 同时发现 style.css 在版本号固定 19:35 后 19:48 又被改过 (mtime 1785671319 > v=1785670500) 且 core.js/agent.js 等 10+ JS 版本号同样过期 → index.html 全部 40 个 `?v=` 参数统一 bump 为 1785682488 强制全量拉新; CDP 实测渐变左右端 #5e69f1→#3f7ff5 + 弹层对齐回归 deltaX=0
- **2026-08-02**: 🐛 **Agent 模式菜单普通模式错位修复 (fixed 包含块劫持)** — 根因: header 毛玻璃 `backdrop-filter: blur(14px) saturate(1.5)` 使 header 成为 `position:fixed` 后代的**包含块**, `_positionModePopup` 的视口坐标 `left` 被整体叠加 header 左偏移 (普通模式侧栏展开 = +258px 错位; Agent 模式侧栏收起 = 0 恰好掩盖); 修复: `#agentModePopup` 移出 header 挂 **body 直下** (与蕾米小窗同根因同方案), 全部逻辑按 ID 引用零改动; Chromium CDP 真实鼠标悬停实测 OFF/AGENT/来回切换 deltaX 均为 0、悬停进入弹出/离开关闭正常
- **2026-08-02**: 🐛 **LongCat max_tokens 超限 400 全链路修复** — LongCat /models 元数据虚高 (context 409600/max_tokens 389120) 越过静态配置 (131072) 致 ModelCap 失效 → 每次请求 400; 修复: `getMaxOutputTokens` 学习值与静态配置取 min (学习值只收紧不放宽)、config.js 输入上限同钳制、400重试正则把限制值模式 (is not less or equal to X / ≤) 排在通用模式前 (原会抓发送值 389120 并持久化错误限制)、stream-handler done 分支在 try/catch(e2){} 外检测残留 buffer 错误 JSON 并抛出 (放 try 内会被静默吞掉); 新增 tests/model_max_tokens.test.js 5 项回归 + 54 项 unittest 通过
- **2026-08-02**: 🧩 **工具/技能面板重复块修复** — 根因是重复渲染只清理卡片与分类、遗漏 Skills/Tools 标题和说明，并错误插到自定义技能列表前；新增专用动态根节点并改为整体幂等替换，同时自动清理旧版直插残留，Chromium 连续 10 次渲染及遗留节点注入回归均保持单份结构
- **2026-08-02**: 🐱 **配置取消换色 + LongCat 全链路修复** — 配置取消改为幂等主题恢复并补移动端快照；LongCat 对齐官方双端点/`LongCat-2.0`/`thinking`，修复 Anthropic SSE 拆包、RS 参数串位与推理-only误完成，默认关闭思考且空正文自动降级重试；真实 OpenAI/Anthropic API、移动浏览器和 52 项单测均通过
- **2026-08-02**: 📱 **iOS Safari 底部视口定稿 + 输入栏收敛** — 重写 `setupKeyboardDetection` 以 visualViewport + 多阶段回稳同步 `--vvh`，消除 Safari 地址栏收起后约 53px 滞留空白并正确识别/恢复软键盘；原样式下压缩输入控件、占位文本单行省略且保留多行自适应，390/360px + 47/34px 安全区模拟实测底距 34px/键盘态 6px、横向溢出 0
- **2026-08-02**: 📱 **iPhone 顶栏控件精准对齐** — 根因是三层移动规则令 Agent/模型/圆形按钮分别约 38/22/29px 且 header 仅 40px；新增末尾几何收敛层，安全区与 44px 内容行分开计算，菜单/Agent/模型/账号/操作控件统一 32px，移动标题从完整 header 下沿定位；375/390px、47px 刘海、登录长用户名实测中心偏差/底部越界/横溢出均为 0，style cache 已更新
- **2026-08-02**: 🧹 **原 UI 保守优化 + 隐性稳定性修复** — 保留并恢复原欢迎字效/圆形输入框/侧栏与面板视觉，仅修复移动标题透明和补齐控件语义；修复工具调用批次折叠失效、图片灯箱监听/滚动锁/重复下载、设置与侧栏跨断点残留、Agent 监听与刷新定时器泄漏、认证用户名转义；JSZip 3.10.1 本地化；cron 新增可取消进程组与线程可靠回收（52 项测试、全量语法与浏览器跨端回归通过）
- **2026-08-02**: 🪟 **蕾米头像放大小窗 (v3: PIP 式持久悬浮窗)** — 点击聊天内可见蕾米头像 → 左下角弹出放大小窗 (222px 毛玻璃卡片, 190px 圆形 GIF 约 4 倍放大), 弹窗期间聊天内小头像全部隐藏 (body.remi-zoom-open), 表情实时同步 (applyMood 联动), 关闭仅 ✕/Esc (v3 移除「点外部自动关闭」— 点输入框不再缩回, 可保持小窗同时打字), 按住头部**可拖动** (pointer 事件, 鼠标+触摸通用, 视口内 clamp 至少留 56px 可抓回, 拖动后位置跨开关保持, 移动端悬浮输入框上方); ⚠️ 容器必须挂 body 直下 (configPanel transform 劫持 fixed 包含块, 曾致不可见+重复副本); model-status.js v12 / style.css v6
- **2026-08-02**: 📱 **顶栏溢出修复 v2 — 登录按钮长用户名撑爆 header** — ①**根因**: `updateAuthHeaderBtn` (index.html) 登录后把完整用户名/邮箱写进 `#authHeaderBtn` (white-space:nowrap), 长用户名 (如邮箱) 使按钮 180px+, 与左容器合计超屏 → header 元素溢出导航栏 (压缩刷新/Agent 后仍溢出); 修复: ≤786px `#authHeaderBtn { max-width:112px; overflow:hidden }` + 用户名 span `max-width:52px; text-overflow:ellipsis` 截断, ≤480px 只保留头像圆 (用户名在个人菜单可见) ②**连带修复**: 上一轮加的 `header .flex.items-center.gap-4 { overflow:hidden }` 会裁剪 agent 模式弹出菜单 (agent-mode-popup absolute 在左容器内) → 移除 overflow, 改 `min-width:0` + `#modelSelect { flex-shrink:1 }` 左容器可收缩不裁菜单; 右容器 `flex-shrink:0` 防变形; 实测 CSS 1752/1752 平衡, 页面正常
- **2026-08-02**: 🐛 **流式代码块撕碎修复 (markdown.js v3 围栏闭合自匹配)** — `_advanceStable` 围栏模式 `indexOf(st.fence, pos)` 中 pos==fenceStart 指向开头 ```, 直接匹配到开头围栏 → 代码块退化成裸 ``` 文本, 刷新后才正常; 修复: 从 `pos + fence.length` 开始搜 ($$ 数学块同修), 含空行代码块/数学块 A/B 单测验证 (5 场景通过)

- **2026-08-02**: 📱 **iOS Safari 视口修复 (底部空白 + 顶栏溢出)** — ①**底部空一大截根因**: iOS Safari 地址栏收起时 `100dvh` 不自动更新, `.h-screen` 停留在"地址栏展开时"的小高度 → 底部空出工具栏高度; 修复: main.js `setupKeyboardDetection` 用 visualViewport 计算布局视口高度写入 CSS 变量 `--vvh` (resize+scroll 监听, 地址栏收起/键盘弹出实时更新), iOS @supports 块 `.h-screen` 改 `height: var(--vvh, 100dvh)` + `html, body { height: 100dvh }` 统一继承链 ②**顶部空一大截根因**: `#chatBox { padding-top: calc(1.5rem + env(safe-area-inset-top)) }` 双重加安全区 (chatBox 顶已在 header 底下方, header 已含 safe) → iOS 顶部多 59px 空白; 改回 `1.5rem` ③**顶栏拥挤/溢出**: 375px 下 header 元素总宽超屏 (`flex-wrap: nowrap` 截断右侧按钮); 修复: 刷新按钮隐藏条件 ≤480→≤786 (刷新进设置面板), `.agent-btn-label` 移动端隐藏 (Agent 按钮图标化), header 左容器 `flex:1 1 auto; min-width:0; overflow:hidden` 兜底; 实测页面加载正常无回归
- **2026-08-02**: 🔄 **页面更新检测 + 硬刷新 (Chrome App/PWA 无刷新按钮场景)** — 新增 `api/version.php` (返回 index.html 内容指纹 mtime+md5, no-store) + `public/js/update-check.js` (60s 轮询 + visibilitychange 即时检查 + 首屏 1.5s 延迟检查, 指纹变化 → 右下角渐变紫蓝胶囊「🔄 新版本可用 · 点击刷新」30s 自动隐藏); `window.hardRefresh()` 清空 Cache Storage + reload (数据管理区新增「强制刷新页面」琥珀色常驻按钮); 项目未注册 Service Worker (sw.js 历史遗留), 故用指纹轮询替代 SW updatefound; 实测: 模拟部署改 index.html → 指纹变化 → 切 tab 触发检查 → 按钮弹出 → 点击 → 页面硬刷新 + 按钮消失 + 新基准无误报
- **2026-08-02**: 📱 **刘海屏/灵动岛适配 (safe-area-inset-top)** — 移动端顶栏被刘海完全遮挡的根因: viewport-fit=cover 后视口延伸到刘海区, header 从视口顶部 0 开始 + chatTitle 胶囊 `top:42px` 硬编码 → 两者都落在刘海区域 (iPhone 灵动岛 safe-area≈59px); 修复: header `padding-top: env(safe-area-inset-top)`, 胶囊 `top: calc(env(...) + 42px)`, chatBox padding-top 同步 calc, 侧栏/配置面板/代理面板顶部也让出安全区 (代理面板 calc(+16px) 保留内容间距); 非刘海屏 env()=0 自动降级零回归
- **2026-08-02**: 🐛 **代码块 Apply 按钮遮挡复制按钮修复** — `addCodeBlockButtons` 原把「📋 Apply」按钮绝对定位 `top:4px right:4px z-index:10`, 与 `.code-actions` 容器 (复制/运行按钮, z-index:5) 完全重叠 → 悬停代码块只看到应用按钮; 改为整合进 `.code-actions` 容器并排 (复用 code-copy-btn 图标样式, 跟随容器悬停显隐), 无容器时 fallback 挂 `top:36px` 避让; utils.js 版本号 bump 强制拉新
- **2026-08-02**: 📱 **移动端竖屏 v3: 蕾米「气泡贴纸」对齐 + 紧凑密度** — 错位根因: `remi-avatar-hide` 用 `display:none` 致头像列时有时无, 最新气泡左移 ~34px; 桌面端改 `visibility:hidden` 保留占位列, 移动端头像改绝对定位贴纸 (脱离文档流零位移, `:has()` 让最新气泡 padding-left 46px), 首帧闪烁消除 (rendering.js 默认隐藏头像); header/消息/输入区全面紧凑化 + 输入 16px 防 iOS 缩放 + viewport-fit=cover; 顺带修复 `.tool-status-bar` 丢失选择器致括号不平衡
- **2026-08-02**: 🎨 **史诗级 UI v2: 侧边栏/顶栏/面板升级** — 顶部 header 毛玻璃(backdrop-filter blur+渐变高光线)+图标按钮弹性动效+模型选择器渐变边框胶囊(自定义下拉箭头); 侧边栏渐变背景+品牌区(渐变图标"对话记录")+新建对话渐变主色按钮+历史条目卡片化(图标+当前项渐变指示条+悬停高亮)+**今天/昨天/更早时间分组**(renderChatHistory)+底部设置玻璃卡片; 配置面板分类卡片化(渐变指示条+open 高亮); 代理面板头部渐变强调; 深浅色全适配+移动端覆盖
- **2026-08-02**: 🎨 **UI/滚动跟随/流式渲染全面优化 v1** — 滚动跟随系统重做 (新模块 `scroll-follow.js`: 移除 #chatBox CSS smooth 与程序滚动的冲突根因、统一 followToBottom 入口、wheel/触摸预处理脱离、滚回底部自动重新吸附、ResizeObserver 异步内容兜底); 流式渲染改增量模式 (markdown.js v3: 稳定块只渲染一次+尾部每帧更新, 围栏/数学块状态跟踪, 流式图片 lazy 不重建, `.streaming` 类泄漏修复, bash `$` 误截断修复); 流结束免 loadChat 全量重建 (finally 就地收尾: finalizeBubbleUI 补操作按钮/页脚); loadChat DocumentFragment + 大列表抑制动画; IME 中文输入 Enter 误发送修复; UI 设计令牌 + 中文字体栈 + 行高 1.65 + /theme 命令 key 统一 + focus-visible 焦点环 + prefers-reduced-motion + 头部 aria-label + textarea 高度统一
- **2026-08-02**: 🔧 **Cloudreve 账号一致性修复 v2.7 (MCP 主账号绑定)** — `cr_getAccessToken('')` 无用户上下文时固定用 `MCP_PRIMARY_EMAIL` 凭据 (`/tmp/cloudreve_login_{md5('mcp_primary')}.json`, 失效自动回退); `login` 主账号同步凭据; `cloudreve_sync.php` email 缺省时查 `users/users.json` 真实邮箱 (不再生成 `{username}@naujtrats.xyz` 幽灵账号); 数据迁移: root(id=42) 名下 16 文件全部改归 xyq070519@gmail.com(id=13), 根目录改名 `root账号遗留文件` 挂载到 13 号根, 分享归属同步; 备份 `.bak_20260802`
- **2026-07-31**: 🤖 **Codex CLI 安装 (DeepSeek-V4-Flash + Responses API)** — codex-cli 0.146.0 官方适配脚本 (config.toml `wire_api="responses"`, base_url=api.deepseek.com); `~/.npm-global/bin/codex` 是代理包装器 (HTTPS_PROXY=socks5h://127.0.0.1:1081 + NO_PROXY=api.deepseek.com,naujtrats.xyz; npm 重装会覆盖包装器); 非 tty 下 `codex exec` 需 `</dev/null`
- **2026-07-30**: 🔧 **股票工具全链路修复** — 空 `properties:{}` 加 `_dummy` 占位符 (DeepSeek schema 校验); 东财强制 IPv4 (monkey-patch getaddrinfo, curl `-4`); 龙虎榜换 `datacenter-web RPT_DAILYBILLBOARD_DETAILS` (旧 API 404); nginx `limit_conn` 10→50
- **2026-07-30**: 📈 **A股股票数据全链路集成 (9工具+可视化)** — `python/engine/stock_data.py` (东财 HTTP+10s缓存+MA/MACD/KDJ/RSI/BOLL+matplotlib 暗色K线图); 引擎路由 + engine_api 9 分支 + MCP 9 定义 + 前端 STOCK_TOOLS/tools-exec/cloudreve.js 分支 + `skills/stock-analysis`
- **2026-07-28**: 🔧 **Agent 三模式重构** — Plan 模式重设计 (只读探索→生成计划→审批横幅→执行→complete 重置); `_planState` 状态机 + `approvePlan/rejectPlan/cancelPlan`; 移除免审按钮 (与 YOLO 重复)
- **2026-07-28**: 🔧 **视觉配置刷新丢失** — `initializeConfig` 补 xAI 视觉配置; `loadConfigFromServer` 守卫视觉字段; 末尾字段可见性切换
- **2026-07-28**: 🔧 **analyze_image 多图片** — 当前消息图片<2张时遍历聊天历史收集所有用户消息 files 去重合并
- **2026-07-28**: 🔧 **xAI 配置刷新丢失** — `loadConfigFromServer` 加 `baseUrlProvider` 守卫; `save_config` 敏感字段保护全部厂商独立 Key
- **2026-07-28**: 🔧 **子代理自动回复** — 移除两处 `isAgentToolsActive()` 守卫; `_triggerMainAgentForTask` 自动切换聊天; `_pendingAgentReply` 忙时标记 (sendMessage finally 触发)
- **2026-07-28**: 🔧 **Agent 权限 + parse_document** — 会话级免审 `toggleSessionAutoApprove` (Agent 弹窗「免审」按钮); 文档解析工具全链路 (引擎端点+engine_api 代理+MCP+前端, 支持 DOCX/XLSX/PPTX/PDF/DOC/TXT)
- **2026-07-28**: 🔧 **analyze_image 全链路修复** — image_proxy.php 剥离 `/oneapichat` 前缀 + 改用 auth_helpers 认证 (原调用不存在的 isLoggedIn → 500); JS 三处 fetch 补 `auth_token`; MiniMax 分支走代理 base64; serverUrl 丢失恢复 (历史 files 匹配/正文 URL 提取); 视觉 Key 空回退主模型; mcp.php 新增 `analyzeImageServerSide()` (DB 解密用户 xAI key) + 无认证自动检测最近活跃用户; `run_skill` 参数映射修复 (`skill_name/params`); skills YAML 模板正文解析修复
- **2026-07-28**: 🔧 **Grok/xAI 视觉修复 (三重根因)** — `sanitizeForLongCat` 跳过含 `image_url` 的数组 (不再转 `[图片]`); main.js content 字符串化加 `supportsVision` 守卫; `detail: 'default'`→`'auto'` (xAI 严格校验)
- **2026-07-28**: 🔧 **xAI 图片下载修复** — xAI/模型名含 grok 强制 base64 data URL 嵌入 (不依赖 xAI 服务器二次下载)
- **2026-07-28**: 🔧 **多图片限制+分批+内存队列** — 单次 xAI≤10张/其他≤20张; queue.js `_splitAndQueueImages` 自动分批 (先识别记忆→最后执行指令); 批量图片走独立内存队列保留完整 base64; `slimSaveChats` 图片保留阈值→200000 字符
- **2026-07-28**: 🔧 **xAI 视觉提供商集成** — 独立视觉提供商下拉 "xAI Vision (Grok)"; `_analyzeImagesWithVisionProvider` 预分析后文本替代图片; analyze_image 工具支持 xAI/OpenAI 分支
- **2026-07-28**: 🔧 **DOCX/PPTX 解压 + 旧版 .doc** — `_isZipBuffer` PK 魔数检测 (非 ZIP 跳过 JSZip); `_extractDocText` UTF-16LE 提取 (中文 CJK+全角标点, 可打印率<50% 丢弃)
- **2026-07-27**: 🔧 **ModelCap max_tokens 智能化** — Grok 全系 8192→131072 (新增 grok-4.5 配置块排在 grok-4 前避免子串误匹配), DeepSeek V3→131072, Claude 4→64000, Gemini→65536; `getMaxOutputTokens` 优先读 AutoAdjust 学习到的 `modelMaxOutputTokens`
- **2026-07-27**: 🔧 **Cloudreve 面板登录状态** — `loadPanel()` 解包 `obj.data || obj` (check_login 的 logged_in 在嵌套 data 内)
- **2026-07-27**: 🔧 **fetchModels 超时** — 代理场景 8s→15s + AbortError 静默重试 1 次; 版本号强制浏览器拉新 JS
- **2026-07-27**: 🔧 **代理智能路由 + OpenCode 移除** — proxy.php `$tryDirect` 标记: 封锁域名 (googleapis/openai/anthropic/x.ai/openrouter/nvidia) 走 Mihomo, 其余直连优先+失败回退; 超时安全网 (流式 900s/非流式 60s + LOW_SPEED 120s); SSE 立即发 `: connected` keepalive; 流式仅未发数据时才回退重试; OpenCode (非 API) 7 处移除
- **2026-07-27**: 🔧 **停止按钮延迟 bug** — `_msgReqGen[chatId]` 请求 ID, 旧请求 finally 检测 ID 不一致直接 return, 不再误删新请求状态
- **2026-07-27**: 🔧 **推入队列按钮** — `injectUserMessage()` 消息立即渲染 (`_injected` 标记+📨 角标), 当前流结束后自动续接; Enter 键仍进队列堆栈
- **2026-07-27**: 🔧 **MCP 工具链全面修复** — mmx_ 路由排在 ENGINE_MAP 前; web_search→新建 `api/v1/search.php` (读用户 Tavily/Brave key); generate_pdf fpdf2 `add_font` 第二参 style; `fixEmptyArrays()` schema 清洗; win_* (7个) SSH 调 Windows 端 win_bridge.py; chaoxing_* (13个) 直调 PHP
- **2026-07-27**: 🔧 **MCP Streamable HTTP + OAuth (OpenClaw)** — `api/v1/mcp.php` 标准协议 (initialize/tools/list/tools/call/ping, 内部翻译 Node.js 协议); `oauth.php` 最小化 OAuth (`oat-*` token); nginx rewrite; 142 工具全加载
- **2026-07-27**: 🔧 **B站依赖 + DeepSeek 流式** — pip 装系统级 `/usr/local/lib/python3.12/dist-packages` (MCP server 以 root 运行找不到用户级依赖); `api.deepseek.com` 加入代理域名列表 (GFW 重置长连接 → SSL_ERROR_SYSCALL)
- **2026-07-27**: 🔧 **代理系统深度修复 + 迁移 WSL2 Mihomo** — 代理开启全走 proxy.php 中继; 27 工具 `properties:{}`→`_dummy`; Mihomo (Clash Meta) systemd 自启, HTTP 1080/SOCKS5 1081/控制器 9090, 阿里云 ECS 禁代理流量故迁 WSL2
- **2026-07-26**: 🔧 **百度网盘扫码登录 (5轮迭代)** — 最终方案: 单会话浏览器 — qr 启动后台进程打开浏览器, 从**页面**提取 QR (页面自己生成 sign), 监测跳转/BDUSS cookie; poll 读状态文件; 期间修复: requests 长轮询 `channel/unicast` (errno:1=未扫) + Playwright 完成登录、先 getqrcode 建立 session、COOKIE_DIR 绝对路径 `/home/naujtrats/mcp-server/netdisk-login`、mcp_proxy 超时 120→300s
- **2026-07-26**: 🔧 **网盘 QR 不可见** — 独立消息行 (`data-qr-login`) 注入聊天容器持久可见, 弹窗仅辅助
- **2026-07-26**: 🔧 **通用文件上传 (.msi/.exe/.zip)** — files.js `isBinary` 检测; upload.php `target=generic` 保留原始扩展名仅禁 Web 脚本; nginx JS 缓存 7天→5分钟 (immutable 导致旧代码不生效)
- **2026-07-25**: 🔧 **Skill 系统整合** — 14→11 个 (bilibili-content-discovery/web-research/document-authoring 合并); `parseSkillFile` 3层嵌套; 系统 prompt 精简只注入 TOP3 匹配技能
- **2026-07-25**: 🔧 **视频猎手全链路 (15工具)** — VIDEO_HUNTER_TOOLS 注入 API + `_mcpExecute` + engine_api `video_hunter`/`bilibili_bridge` 代理路由; IIFE 作用域 bug 修复 (必须 `window.` 前缀); B站 yt-dlp 用 `.bili_cookie` 生成 Netscape Cookie 文件; DASH 下载独立 `dash-downloader.py` (start_new_session 防 daemon 线程被杀); LongCat 跳过 `reasoning_content` 非标准字段 (apply_chat_template 报错)
- **2026-07-25**: 🔧 **DeepSeek max_tokens 400** — AutoAdjust 正则取 `match[2]` (max) 而非 min; deepseek-v4-pro/flash/reasoner `maxOutputTokens` 1M→131072
- **2026-07-25**: 📂 **网盘解析集成 (9种网盘)** — `api/netdisk_api.php` + `python/netdisk/netdisk_parser.py` + MCP 4 工具 + 前端 NETDISK_TOOLS/netdisk.js + skill; aria2 16线程下载到 `uploads/downloads/`; 🔑 网盘扫码登录 (baidu/quark/aliyun 独立脚本 + netdisk_login 工具 + cookie 共享)
- **2026-07-23**: 🗺️ **高德地图 Skills (ClawHub @lbs-amap)** — `api/amap_api.php` 14 action + MCP 14 工具 (ENGINE_MAP `__amap/`) + key 配置 (`config/amap_config.php`)
- **2026-07-23**: 🔧 **MCP 全量转移 + 星穹铁道清理** — 42 工具注册 MCP (89→120); SRC_TOOLS/入口按钮移除改 Cloudreve 云盘
- **2026-07-23**: 🔧 **可恢复流式传输 (RS) 第二轮** — init.js `_executeOrphanedToolCalls` 续接前先补 tool results (避免 400); resume-stream `_resumeToolHandoff`; LongCat `chunk.choices[0]` 空数组守卫; Anthropic 格式 RS 支持 (`_generate_resumable_anthropic`)
- **2026-07-22**: 🐱 **LongCat.Chat 集成** — API_PROVIDERS 条目 (baseUrl `https://api.longcat.chat/openai/v1`); fetchModels 自定义 modelsUrl; 不支持 CORS 走中继
- **2026-07-22**: 🧠 **记忆系统 v2 (方案B)** — `memory_db.py` (SQLite FTS5 事实/情节/人格) + `memory_engine.py` (chromadb+fastembed, 向量×0.4+BM25×0.3+图谱×0.2+重要性+时间衰减 RRF 融合) + 18 端点 + `personality.py` 8 预设; engine_api 20 代理路由; 旧系统保留兼容
- **2026-07-22**: 🔧 **P0 子代理内容接入** — `_run()` 结束后结构化 JSON 输出 (`_structured` 字段); `agent:result` SSE 广播; 轮询 15s→30s 降级为后备
- **2026-07-22**: 🔧 **Cloudreve v2.6.3/v2.6.2/v2.6/v2.6.1** — cr_check_login; cr_upload_file 两步上传分片 (25MB); cr_create_folder path 兼容; cr_wait_path 父目录回退; RS 默认关 (`=== '1'` 才启用); slimSaveChats >3.5MB 存服务器
- **2026-07-21**: 🔧 **Cloudreve v4 修复** — `$isMcpCall` 放行 `auth_token=cr_shared`; copy→`/file/move`+copy:true; download→`/file/url`; delete 接受 path 单数
- **2026-07-20**: 🖥️ **Windows 桌面客户端** — Electron 壳→HTTPS 远程页面 (服务器零改动); 托盘/服务器切换/外部链接走浏览器; `deploy/` + release.yml 自动构建
- **2026-07-20**: 🕐 **时间感知增强** — `get_current_time` 内置工具; 关键词注入 4 层 (精确/事件/时段/日期); 6 技能加时间感知章节
- **2026-07-19**: 🌐 **API v1 重构 + 第三方注入** — tools/call.php 透明代理 69 工具 (MCP 作通用后端); tools.php 动态加载; chat/completions 注入引擎+MCP 合并 46 工具
- **2026-07-19**: 🔐 **超星扫码登录** — `chaoxing-login.py` (check/qr/login); QR 消息行渲染; poll 预加载 JSESSIONID
- **2026-07-17**: 🌐 **公共 REST API** — API Key `oac-<48hex>` SHA-256 存储; OpenAI 兼容端点; api_keys.php 管理; API.md
- **2026-07-17**: 🔧 工具执行+MCP 全量适配 — v1 全部 27 工具 + `_decrypt_config_key()`
- **2026-07-16**: 🔧 Gemini+超星修复 — 代理域名加入 googleapis; proxyUrl 映射 `192.168.195.213:10808`; 0 字节配置重建
- **2026-07-15**: 🔍 Tavily 修复 + Gemini thought_signature + Anthropic 格式 — `search_depth` key 前缀检测; 三处全链路保留 thought_signature; `useAnthropicFormat` 开关 (Claude 自动启用, Anthropic 格式禁 RS)
- **2026-06-22**: 🔧 fetch.php 代理+429 退避重试; 配置跨设备同步 `_scheduleConfigSync`; Agent 聊天跨设备同步; engine_push URL 净化
- **2026-06-13**: 🔧 综合修复 — RS+代理 URL 映射; duplicate tool_call_id 去重; MiniMax `(think)` 提取; max_tokens 自动减小
- **2026-06-12**: 🔒 强制认证 (chat/engine 401); MCP mmx 路由; 超星 cookie-first; browser click/type 三级降级; 搜索全走服务器代理
- **2026-06-11**: 🤖 Skills 系统 + 网页抓取增强
- **2026-06-10**: 🐛 RAG+Mermaid+MiniMax markdown 修复
- **2026-06-08**: 🐛 RS 刷新+刷课模块修复
- **2026-06-06**: 📦 Phase 0-9 代码拆分 + 🔒 安全升级 + ⚡ 懒加载

## 项目概览

多模型 AI 聊天客户端，深度集成超星学习通自动化、Agent 子代理系统、视频编辑、Cloudreve 云盘。

- **域名**: `https://naujtrats.xyz/oneapichat/`
- **许可证**: GPL-3.0
- **主分支**: `main`

## 技术栈

| 层 | 技术 |
|---|---|
| 前端 | 原生 JS SPA（单文件 ~20K 行）、Tailwind CSS、KaTeX、Mermaid、Service Worker PWA |
| 后端 API | PHP 8.3 + php-fpm，JSON 文件存储 |
| 引擎 | Python FastAPI（端口 8766）、SQLite + JSON 文件存储 |
| 自动化 | Python 子进程（超星刷课/考试）、Flask + Celery（端口 8082） |
| 服务器 | Nginx 反向代理、Let's Encrypt SSL、Ubuntu WSL2 |

## 目录结构

```
/var/www/html/oneapichat/
├── api/              # PHP API 端点（认证、聊天、配置、引擎代理等 20+ 文件）
├── public/           # 前端静态资源
│   ├── index.html    # 主聊天 SPA 入口
│   ├── chaoxing.html # 超星刷课/考试面板
│   ├── js/core.js    # ★ 核心运行时 — 全局常量、DOM工具、加密、Cookie (Phase 0)
│   ├── js/main.js    # 主应用逻辑（~5,600 行，聊天/Agent/UI/工具执行）
│   ├── js/models.js  # 模型定义
│   ├── css/style.css # 主样式表
│   └── lib/lib/      # 第三方库（KaTeX、marked、mermaid、xlsx 等）
├── python/           # Python 后端
│   ├── engine_server.py # ★ 核心引擎（FastAPI, Agent, 流式, SSE, 视频, 浏览器）
│   ├── chaoxing/     # ★ 超星/考试模块（刷课、答题、考试、字体解密）
│   │   ├── main.py   # 超星自动化主入口
│   │   ├── scripts/  # 考试浏览器自动化脚本
│   │   └── learning_records.db  # 学习记录
│   └── engine/       # 引擎模块（浏览器、事件、策略、重试）
├── users/            # 用户数据（users.json, sessions.json, 配置, 记忆）
├── chat_data/        # 聊天历史 JSON 文件
├── .engine/          # 引擎运行时（SQLite 聊天 DB、Agent/Cron 状态）
├── uploads/          # 用户上传文件
├── deploy/           # 部署脚本、Docker、Nginx 配置
├── docs/             # 文档（README, CHANGELOG, LICENSE）
├── config/           # 配置文件（.mmx_config.json）
└── workspace/        # Agent 工作目录
```

## 服务端口与路由

| 路径 | 后端 | 端口 | 说明 |
|---|---|---|---|
| `/oneapichat/` | Nginx 静态 + PHP-FPM | 80/443 | 主应用 |
| `/engine/` | Python FastAPI | 8766 | Agent 引擎、SSE、流式 |
| `/rag/` | RAG 服务 | 8765 | 知识库检索 |
| `/mcp/` | Node.js MCP | 18788 | MCP 协议服务 |
| `/src/`, `/srcwebui/` | Flask | 8082 | 星穹铁道自动化 |
| `/py/` | Flask | 8082 (远程) | Python 后端 |

## 关键 PHP API

| 文件 | 功能 |
|---|---|
| `api/auth.php` | 用户认证（注册/登录/Token/邮箱验证） |
| `api/chat.php` | 聊天 CRUD + 用户配置同步 |
| `api/config.php` | 用户设置保存/加载 |
| `api/chaoxing_api.php` | 超星自动化枢纽（课程列表、刷课、考试） |
| `api/engine_api.php` | ★ 引擎代理（Agent/工作流/SSE 广播/浏览器/文件/MiniMax） |
| `api/proxy.php` | API 代理中继 |
| `api/upload.php` | 文件上传 |
| `api/fetch.php` | URL 抓取代理 |
| `api/memory_api.php` | 跨会话记忆系统 |
| `api/cloudreve_api.php` | Cloudreve 云盘桥接 |

## 核心 JS 模块

| 文件 | 行数 | 内容 |
|---|---|---|
| `js/core.js` | 309 | ★ 全局常量、数学公式保护、跨域Cookie、安全Fetch、DOM工具、加密、工具函数 |
| `js/image-gen.js` | 490 | 图像生成（generateImage / generateImageI2I / OpenRouter GPT Image） |
| `js/markdown.js` | 490 | 流式渲染（applyStreamRender）、MarkdownRenderer缓存、ChartRenderer/Mermaid |
| `js/main.js` | 3,116 | 主应用逻辑（聊天、Agent、工具执行） |
| `js/stream-handler.js` | 1,238 | 流式/非流式响应处理 (Phase 10 拆分) |
| `js/tools.js` | 1,502 | 工具定义、toolRegistry、工具分类和中文标签 |
| `js/commands.js` | 210 | /斜杠命令解析与分派 (Phase 9 拆分) |
| `js/resume-stream.js` | 158 | 可恢复流式续接模块 (Phase 9 拆分) |
| `js/models.js` | ~300 | 模型配置适配 |

- **聊天流**: `sendMessage()` → `attemptRequestWithFreshAbort()` → SSE/非流式处理
- **ResumeStream**: 可恢复流模块 — 刷新后从引擎续接（`ResumeStream.create/resume/resumeByStreamId`）
- **SSE 事件总线**: `connectSSEChannel()` → 跨浏览器实时同步（`/engine/events`）
- **Agent 审批**: `requestToolApproval()` → YOLO 自动 > 临时授权 > Plan 拒绝 > 弹窗
- **任务恢复**: `_recoverActiveTasks()` → 刷新后从 SQLite 恢复活跃流
- **消息队列**: `window._messageQueue` → localStorage 持久化 + 500ms 节流
- **402 降级**: 自动提取可负担 token 数，降低 `max_tokens` 后重试
- **临时权限**: `_tempAgentGranted` → ask_agent 单次授权 + 绿灯呼吸动画

## 引擎 API（engine_server.py）

| 端点 | 方法 | 功能 |
|---|---|---|
| `/engine/health` | GET | 健康检查 |
| `/engine/chat/create` | POST | 创建可恢复流 → 返回 `stream_id` |
| `/engine/chat/stream/{id}` | GET | 消费 SSE 流（断线续传） |
| `/engine/events` | GET | ★ 用户级 SSE 通道（跨浏览器实时推送） |
| `/engine/events/broadcast` | POST | 广播事件到同用户其他连接 |
| `/engine/tasks/active` | GET | 获取活跃任务（跨浏览器恢复） |
| `/engine/agent/*` | CRUD | Agent 管理（创建/运行/状态/停止/通知） |
| `/engine/cron/*` | CRUD | 定时任务管理 |
| `/engine/workflow/*` | CRUD | 工作流管理 |
| `/engine/file_*` | CRUD | 文件操作（read/write/search/op） |
| `/engine/heartbeat` | GET/POST | 心跳 + 推送 |
| `/engine/mmx` | POST | MiniMax 多模态（TTS/图片/视频/搜索） |
| `/engine/browser_*` | CRUD | 浏览器自动化 |
| `/engine/video_edit` | POST | 视频编辑管道 |

## 系统服务

| 服务 | 状态 |
|---|---|
| `php8.3-fpm` | active |
| `nginx` | active |
| `oneapichat-engine.service` | Python 引擎（8766） |
| `oneapichat-py.service` | Flask 后端（8082） |
| `chromium-cdp.service` | 浏览器自动化 |

**守护脚本**: `/var/www/html/oneapichat/engine_watchdog.sh`（每分钟 cron 检查引擎健康）

## 数据存储

| 位置 | 内容 | 格式 |
|---|---|---|
| `users/users.json` | 用户账户 | JSON |
| `users/sessions.json` | 会话 Token | JSON（30 天过期） |
| `chat_data/user_*_*.json` | 聊天历史 | JSON（per-user + per-chat） |
| `.engine/chat_*.db` | 流式进度 + 活跃任务 | SQLite |
| `.engine/memory/` | Agent 记忆/人格 | JSON |
| `python/chaoxing/learning_records.db` | 超星课程进度 | SQLite |
| `uploads/` | 用户文件 | 文件系统 |

## 已知问题与注意事项

1. `public/` 下 `lib/`、`resource/`、`src/` 多一层嵌套（`lib/lib/` 等），项目根有符号链接指向内层
2. `index_root.html` 为独立入口（已从 git 恢复），通过根符号链接访问
3. `python/chaoxing/search_question.py` → 根 `api/search_question.py` 通过符号链接访问
4. 密钥硬编码：AES 密钥 `naujtrats-secret` 在 `api/init.php` 和 `main.js` 中
5. 会话 Cookie 域 `.naujtrats.xyz` 用于跨子域共享
6. `keepalive: false` 用于配置保存 fetch（因 body 可能超 64KB）
7. 引擎重启时旧的 `_resumable` 内存状态丢失，但 SQLite `active_tasks` 表保留

## Task Master AI Instructions

**Import Task Master's development workflow commands and guidelines, treat as if import is in the main CLAUDE.md file.**
@./.taskmaster/CLAUDE.md
